Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

Inline comments:
In `@backend/app/api/v1/endpoints/marketplace.py`:
- Around line 221-245: The current implementation has a race condition where the
user's like status (liked_ids) and the likeCount are updated in separate
operations. When multiple concurrent requests from the same user occur, they can
all read the same initial state before any updates are written, causing the
likeCount to increment multiple times while liked_ids only reflects one entry.
Consolidate the updates by using a single atomic operation that handles both the
likeCount update via _updater and the liked_ids update via
marketplace_stats_store.mutate or marketplace_stats_store.set in a single
transaction, ensuring that the read of current like status and all subsequent
writes for both the itemId stats and the likes_key are performed atomically
together.
- Around line 75-87: The exception handler in the install_marketplace_item
endpoint does not update the task status when URL validation fails in
_validate_download_url or other errors occur before successful installation.
Since the task is pre-written to queued status, validation failures leave it
stuck in the queued state, causing the progress interface to perpetually return
queued and blocking subsequent installation attempts. In the except block that
catches exceptions, add code to update the task status from queued to a failed
state (such as failed or error) so that the progress interface correctly
reflects the validation or installation failure and allows users to retry or
take appropriate action.

In `@backend/app/api/v1/endpoints/repo_source.py`:
- Around line 212-223: The code incorrectly reuses sub-market IDs based on array
index position, which breaks when sub-markets are reordered or deleted, causing
the wrong ID to be assigned to a sub-market. Fix this by requiring the update
request to include the id field in each sub-market object. In the list
comprehension that builds update_data["sub_markets"], instead of using
existing_sms[i]["id"] to match by position, match the existing sub-market by
looking up the id from sm (the current sub-market in the request). If id is
missing from sm, match by a stable key like name or url from the existing
sub-markets, or generate a new id if no match is found.

In `@backend/app/infrastructure/install/install_service.py`:
- Around line 266-285: The third return value from _find_item_source (currently
passed as download_url to _download_github_dir) is being treated as a GitHub
contents API path, but item.get("downloadUrl", "") may return a complete URL or
be empty, causing incorrect downloads. Clarify the contract: if downloadUrl is
intended to be a repository-relative directory path, extract only the path
component from any full URL returned by _find_item_source; if it can
legitimately be a complete URL, add separate logic before calling
_download_github_dir to detect when download_url is a full URL and use a remote
archive download method instead of the GitHub contents API path approach. This
fix should be applied to both the occurrence around line 266-285 and the
additional location mentioned at lines 340-362.
- Around line 333-337: The file download logic in the section where file_resp is
checked only handles the successful 200 status code case but silently skips any
non-200 responses. This causes incomplete packages to be generated and
installed. Add error handling after the if file_resp.status_code == 200 block to
check for unsuccessful responses, and raise an exception with details about the
download failure (including the status code and URL) so that the failure
propagates back to the installation task instead of allowing the installation to
proceed with missing files.
- Around line 405-417: Add validation limits to the zip extraction logic in the
InstallService to prevent resource exhaustion attacks. Before the loop that
iterates through zf.infolist() entries, initialize counters for total
decompressed size and entry count. Inside the loop, before processing each
entry, accumulate the entry.file_size and check if it exceeds reasonable limits
(both individual file size and total cumulative size, as well as maximum allowed
entry count). If any limit is exceeded, log a warning and skip that entry
similar to how path traversal is currently handled. This prevents malicious zips
with oversized files or excessive entries from consuming disk space or memory.
- Around line 165-166: The httpx AsyncClient with `follow_redirects=True`
automatically follows HTTP redirects without re-validating the target URL,
creating an SSRF vulnerability where attackers can redirect from a validated
public URL to internal addresses. To fix this: disable `follow_redirects=True`
in the AsyncClient initialization, then manually handle redirects by checking
the response status code for 3xx responses, extracting the Location header,
validating the redirect target URL using the same `_validate_download_url`
validation function that was used for the initial URL, and only following the
redirect if validation passes.

In `@backend/app/infrastructure/sync/github_sync.py`:
- Around line 267-269: The ID generation in the `item.id` assignment only uses
`item.name` for the MD5 hash, which causes collisions when multiple items have
the same name across different sources or types. Include stable source
information (such as repository URL, source identifier, or other unique context)
along with `item.name` in the hash seed to make the ID generation more robust.
Additionally, add duplicate ID detection logic within the current
parsing/synchronization flow to identify and handle collisions, ensuring that
conflicting IDs are either logged, skipped, or resolved with additional
disambiguation.

In `@frontend/src/renderer/src/views/MarketDetailView.vue`:
- Around line 143-146: When the downloadUrl is not available and installError is
being set in the block with the !downloadUrl check, you must also set errorType
to 'install' at the same time. Currently only installError.value is being set to
the error message, but errorType remains unchanged from its previous value,
which can cause the retry button to incorrectly execute the uninstall flow
instead of install flow if the previous state was an uninstall failure. Add the
assignment of errorType to 'install' alongside the installError.value assignment
to ensure proper state synchronization.

---

Outside diff comments:
In `@backend/app/api/v1/endpoints/marketplace.py`:
- Around line 60-65: The InstallRequest class currently accepts an optional
downloadUrl field from the client, which bypasses the trusted marketplace
metadata verification. Remove the downloadUrl field from the InstallRequest
class definition (between itemName and the version field). Then update the
installation handler that processes InstallRequest to fetch the actual download
URL from trusted marketplace metadata by querying with itemId and itemType
instead of using any client-provided URL, ensuring all downloads come from
verified marketplace sources.

In `@backend/app/api/v1/endpoints/repo_source.py`:
- Around line 224-226: When updating sub_markets data in the source object
(around the repo_sources_store.set call after line 226), you need to clear the
associated cache entries to prevent stale data from being served. After the
repo_sources_store.set(source_id, source) call completes, invoke the source
prefix cache cleanup logic that was newly added in this PR to remove any cached
entries with the source_id prefix pattern (such as source_id::subMarketId
entries) to ensure that if a sub-market URL changed and its ID was reused, the
old cached repository data won't be served on subsequent installations or
displays.

In `@backend/app/infrastructure/install/install_service.py`:
- Around line 391-409: The target_dir is constructed from user-controllable
item_id without validation, so if item_id contains "../", the directory can
escape install_dir despite secure zip extraction. Add a validation check after
creating target_dir and before the try block that resolves target_dir and
verifies it is relative to the resolved install_dir using is_relative_to()
method. Raise a security exception if target_dir would escape the install_dir,
preventing the backup and extraction from proceeding with malicious item_id
values.

In `@新建` 文本文档.md:
- Around line 1-424: This markdown document is missing critical metadata and
structure needed for code review purposes. Add a title section at the top with
timestamp, environment details, and a "Status" indicator (e.g., "Before Fix" or
"After Fix") to clarify when this error occurred. Below the metadata, add a
"Context" section explaining the problem source, reproduction steps, and the
root cause. Then reorganize the error logs by grouping the stack trace into
logical sections with headers (e.g., "Initial Error", "Cascade Errors") rather
than dumping them as raw unformatted text. Finally, add a "Resolution" section
at the end explaining what action was taken to fix the issue or what the next
steps are. This documentation currently cannot serve as valid PR review evidence
without these structural improvements.

---

Nitpick comments:
In `@backend/app/api/v1/endpoints/marketplace.py`:
- Around line 219-250: The async function toggle_like lacks a return type
annotation on the function signature. Add a return type hint to toggle_like (and
similarly to get_item_stats and get_all_stats functions) since these are API
boundary endpoints. At minimum, add -> dict as the return type annotation, but
preferably define and use a proper Pydantic response model to explicitly
constrain and document the structure of the returned dictionary (containing
fields like itemId, isLiked, and likeCount).

In `@backend/app/infrastructure/database/json_store.py`:
- Around line 92-100: The mutate method in the json_store.py file lacks proper
type annotations for both the updater_fn callback parameter and the method's
return type. Add a type hint for the updater_fn parameter to specify what type
of callback is expected (it should accept an optional value and return the new
value) and add a return type annotation to the mutate method to indicate what
type is being returned. This will enable static type checking and allow callers
to properly validate their usage of this atomic update entry point.

In `@frontend/src/renderer/src/stores/marketplace.ts`:
- Line 609: Remove the explicit `any` type annotation from the callback
parameter in the installedItems.find() method call. Since installedItems already
has an explicit type defined earlier in the code, the callback parameter type
can be safely inferred by TypeScript. Replace `(i: any)` with `(i)` to allow
proper static type checking and maintain type safety across the marketplace
store.