Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

Inline comments:
In `@backend/app/api/v1/endpoints/chat.py`:
- Around line 715-718: The current logger.info in the search_conversations
endpoint logs the raw user-provided keyword (sensitive), so change logging to
avoid recording the full keyword: in the function search_conversations
(decorated by `@router.get`("/conversations/search")) remove the raw keyword from
the log and instead log non-sensitive metadata such as keyword length, agent_id
presence/obfuscated value, a generated request id, and the number of hits
returned by conversation_store.search_conversations; ensure you still call
conversation_store.search_conversations(keyword, agent_id) but only log safe
derived info (e.g., len(keyword), results count, and request id) via
logger.info.
- Around line 888-901: The auto-title update only checks for conv.get("title")
== "新对话" but create_conversation() may default to "New Conversation", so the
rename never runs for API/old clients; update the logic in the handler that
performs the auto-update (the block using conv and first_user_message) to treat
both default values or, better, reference a single DEFAULT_TITLE constant used
by create_conversation() and the auto-update code; change the check to compare
against that constant (or both "新对话" and "New Conversation") so conv["title"] is
reliably detected and replaced with the truncated first user message.
- Around line 925-944: Align stream types and fix title/logging bugs: update
provider/runtime type annotations so chat_stream() returns
AsyncIterator[StreamEvent] (replace AsyncIterator[dict|str] in
backend/app/runtime/provider/llm/adapter.py and
backend/app/runtime/provider/base.py) so _STREAM_RESPONSE (uses
chunk.data.get(...)) matches the contract; unify conversation default title
values used by create_conversation and the _PHASE_3_SAVE_ASSISTANT_MSG check
(make both use the same literal, e.g., "New Conversation" or "新对话") so automatic
renaming triggers correctly; and remove or sanitize logging of raw search input
in the conversations/search handler (avoid logger.info(... keyword={keyword}
...) or redact sensitive parts before logging).

In `@backend/app/core/agent/tool_loop.py`:
- Line 134: The log currently prints raw tool parameters via
logger.info(f"[ToolLoop] calling {tool_name}({args_str[:100]})"), which may leak
sensitive user data; change the logging to only include the tool name and the
parameter keys (not values). In the ToolLoop call site (look for tool_name,
args_str, args), replace usage of args_str with a sanitized summary such as a
sorted/joined list of argument keys (e.g., keys = sorted(args.keys()) or keys =
list(args.keys())) and log logger.info(f"[ToolLoop] calling {tool_name} with
params: {keys}") so no raw parameter values are written to logs.
- Around line 384-392: collected_tool_calls is iterated in insertion order which
can differ from the model's original index; instead, sort the items by each
value's "index" (e.g., v.get("index", 0)) before building tool_calls_list so
tool calls reflect the model's intended order. Locate the code that constructs
tool_calls_list from collected_tool_calls and change the iteration to use a
sorted list (sorted(collected_tool_calls.items(), key=lambda kv:
kv[1].get("index", 0))) and then assemble the same dict entries (using "id",
"type", "function") in that sorted order to ensure stable ordering.

In `@backend/app/engines/memory/core/injector.py`:
- Around line 161-170: 在 _format_recent_context 方法中，当传入 thread_id 并使用
wm.get_conversations_for(thread_id) 时不应注入全局摘要 wm.conversation_summary；定位到函数
_format_recent_context、变量 memory_data、thread_id 以及调用
wm.get_conversations_for(thread_id)。修复方式：在有 thread_id 的分支里改为使用线程级摘要（例如从 wm
的线程对象或方法读取该 thread 的 summary）并通过现有 _sanitize_content 处理后加入
sections，若线程级摘要不可用则完全跳过添加全局 wm.conversation_summary，以避免跨会话上下文泄露。

In `@backend/app/engines/memory/core/models.py`:
- Around line 122-124: thread_conversations and thread_core_goals are unbounded
dicts keyed by thread_id and can grow without limit; change these storages to
enforce a maximum number of threads and evict old entries (e.g., LRU) when the
limit is exceeded. Replace or wrap the current dicts created by
Field(default_factory=dict) with a bounded LRU mapping (or an OrderedDict +
eviction logic) and apply the same bounded-eviction approach to the other
per-thread dicts referenced in the file (the maps around lines 137-148 and
154-162), ensuring creation/update paths for thread_conversations and
thread_core_goals update LRU access order and trigger eviction of the oldest
thread entries (removing all associated per-thread state) when the global thread
count exceeds the configured limit.

In `@backend/app/runtime/plugin/skill/registry.py`:
- Around line 304-307: The code currently checks task_description with
kwargs.get and returns raw exception text to users; change the validation to
strip whitespace (e.g., task_description = kwargs.get("task_description",
"").strip()) and treat empty strings as invalid, returning
SkillResult(success=False, error="任务描述不能为空"). For the exception handling around
the block that now concatenates the original exception (lines near 317-318),
stop exposing the exception string to the client: log the full exception
internally (using the existing logger) and return a generic error via
SkillResult (e.g., SkillResult(success=False, error="处理任务时发生错误")) so internal
details are not leaked. Ensure you reference and update the same symbols:
task_description, SkillResult, and the exception handling code path.

In `@backend/app/utils/tool_executor.py`:
- Around line 40-52: 在委托工具分支中对 task_description 只做 truthy 检查会放过仅包含空白的字符串，且在
except 分支直接把异常文本返回会泄露内部信息；在包含 delegate_sub_task 的 try/except 块中先对
args.get("task_description", "") 做 .strip() 并以该 strip 后的值做空值判断（若为空返回用户友好信息如
"任务描述为空" 或类似提示），在 except 中不要返回原始异常字符串，而是返回或抛出通用的失败提示（例如
"子代理执行失败"），同时在服务器日志中记录详细异常信息以便排查，确保引用的标识符为 task_description 和 delegate_sub_task
以便定位修改位置。

In `@frontend/src/renderer/src/components/LumiSidebar.vue`:
- Around line 39-50: watch(searchQuery, (q) => { ... }) currently cancels only
the timer but doesn't prevent earlier async responses from overriding newer
results; add a simple sequencing check: keep a module-scoped incrementing token
(e.g., searchSeq) that you increment before starting the setTimeout and capture
into a local variable inside the async callback, then after awaiting
chatStore.searchConversations(q) only assign to searchResults.value and set
isSearching.value = false if the captured token matches the latest searchSeq;
reference searchTimer, searchSeq (new), chatStore.searchConversations,
isSearching, and searchResults to locate where to add the token check and update
logic.
- Around line 119-125: The snippet highlighting function highlightSnippet
currently injects raw HTML into the template via v-html after only wrapping
matched terms with <mark>, which leaves existing HTML in result.snippet
unescaped and introduces XSS; fix by HTML-escaping the original snippet content
before applying the highlight replacement so only the injected <mark> tags are
trusted—i.e., locate highlightSnippet and ensure you escape special chars in
snippet (convert &, <, >, ", ') prior to building the regex replacement, then
perform the highlight on the escaped text and let the template render the safe
HTML with v-html; apply the same escaping flow to the other usage noted around
lines 252-253.
- Around line 128-133: selectConversation 在写入 chatStore.searchScrollTarget，但
chat store（chat.ts）未声明该字段，导致类型检查和运行时读取失败；在 chat store（chat.ts）中为
searchScrollTarget 添加响应式状态和类型（例如可为 { convId: string; keyword: string } | null
的字段），并导出/包含到 chatStore 对象，或将 selectConversation
中的赋值移除以避免访问未声明属性；确保相关使用点（selectConversation 和
chatStore.loadConversation）根据新字段的可空性做适当空值检查。