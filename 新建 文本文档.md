Verify each finding against current code. Fix only still-valid issues, skip the
rest with a brief reason, keep changes minimal, and validate.

In `@backend/app/engines/memory/memory_engine.py` around lines 39 - 49, The
MemoryEngine is not setting self._agent_id, causing build_context() to default
to "_default" and share storage across agents; update MemoryEngine.__init__ to
accept an optional agent_id: str | None = None (or required if preferred) and
set self._agent_id = agent_id or "_default" so instance-specific paths are built
correctly, then propagate this change to other constructors/usages noted (the
blocks around lines 178-184 and 312-323) so all MemoryEngine instances created
via get_memory_engine("agent-x") receive and store the correct agent_id.