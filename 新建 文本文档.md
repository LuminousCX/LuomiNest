# 历史记录日期分组 完整重写方案
**现有问题：分组逻辑重叠、时间粒度混乱、日期显示不一致**。按照"宁肯重写不缝补"原则，直接删掉原来错误的分组代码，重新实现一套**行业通用、逻辑清晰、无重叠**的日期分组系统。

---

## 一、现有问题分析
从截图可以看到明显的逻辑错误：
1. **分组重叠**：5月25日的消息同时出现在"前天"和"本周"两个分组
2. **粒度混乱**：既有按天的"今天/昨天/前天"，又有按周的"本周/上周"，边界模糊
3. **显示不一致**：有的显示相对时间（昨天），有的显示绝对日期（5月26日）
4. **排序错误**：时间倒序不连贯，用户需要手动换算日期

---

## 二、正确的行业标准分组逻辑
**核心原则：分组互斥，一个消息只属于一个分组**，按时间从近到远排序：

| 分组名称 | 时间范围 | 组内日期显示 |
|----------|----------|--------------|
| 今天 | 00:00 - 现在 | 15:04 |
| 昨天 | 昨天00:00 - 昨天23:59 | 昨天 14:33 |
| 近7天 | 3天前 - 7天前 | 周三 10:20 |
| 本月 | 本月1日 - 8天前 | 5月20日 |
| 上月 | 上月全月 | 4月15日 |
| 更早 | 上月之前 | 2026年3月 |

---

## 三、完整实现代码
### 1. 新增日期分组工具函数
在 `frontend/src/renderer/src/utils/date.ts` 中添加：
```typescript
/**
 * 日期分组工具函数
 * @param dateStr ISO格式日期字符串
 * @returns 分组信息
 */
export function getDateGroup(dateStr: string): {
  name: string;
  sort: number; // 分组排序权重，越小越靠前
  displayTime: string; // 组内显示的时间
} {
  const date = new Date(dateStr);
  const now = new Date();
  
  // 重置时间到0点，用于日期比较
  const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  const targetDay = new Date(date.getFullYear(), date.getMonth(), date.getDate());
  
  // 计算天数差
  const diffDays = Math.floor((today.getTime() - targetDay.getTime()) / (1000 * 60 * 60 * 24));
  
  // 格式化时间
  const timeStr = date.toLocaleTimeString('zh-CN', { hour: '2-digit', minute: '2-digit' });
  
  // 分组判断
  if (diffDays === 0) {
    return {
      name: '今天',
      sort: 0,
      displayTime: timeStr
    };
  } else if (diffDays === 1) {
    return {
      name: '昨天',
      sort: 1,
      displayTime: `昨天 ${timeStr}`
    };
  } else if (diffDays <= 7) {
    // 近7天显示星期几
    const weekdays = ['周日', '周一', '周二', '周三', '周四', '周五', '周六'];
    return {
      name: '近7天',
      sort: 2,
      displayTime: `${weekdays[date.getDay()]} ${timeStr}`
    };
  } else if (date.getMonth() === now.getMonth() && date.getFullYear() === now.getFullYear()) {
    // 本月
    return {
      name: '本月',
      sort: 3,
      displayTime: `${date.getMonth() + 1}月${date.getDate()}日 ${timeStr}`
    };
  } else if (date.getMonth() === now.getMonth() - 1 && date.getFullYear() === now.getFullYear()) {
    // 上月
    return {
      name: '上月',
      sort: 4,
      displayTime: `${date.getMonth() + 1}月${date.getDate()}日 ${timeStr}`
    };
  } else {
    // 更早
    return {
      name: date.getFullYear() === now.getFullYear() 
        ? `${date.getMonth() + 1}月` 
        : `${date.getFullYear()}年${date.getMonth() + 1}月`,
      sort: 5,
      displayTime: `${date.getFullYear()}年${date.getMonth() + 1}月${date.getDate()}日`
    };
  }
}

/**
 * 将对话列表按日期分组
 * @param conversations 对话列表，每个对话必须有 created_at 字段
 * @returns 分组后的对话列表
 */
export function groupConversationsByDate(conversations: any[]) {
  const groups: { [key: string]: { name: string; sort: number; conversations: any[] } } = {};
  
  for (const conv of conversations) {
    const group = getDateGroup(conv.created_at);
    const groupKey = `${group.sort}-${group.name}`;
    
    if (!groups[groupKey]) {
      groups[groupKey] = {
        name: group.name,
        sort: group.sort,
        conversations: []
      };
    }
    
    // 给每个对话添加显示时间
    conv.displayTime = group.displayTime;
    groups[groupKey].conversations.push(conv);
  }
  
  // 按排序权重排序分组
  return Object.values(groups).sort((a, b) => a.sort - b.sort);
}
```

### 2. 修改历史记录页面
在你的历史记录组件中，替换原来的分组逻辑：
```vue
<template>
  <div class="history-list">
    <div v-for="group in groupedConversations" :key="group.name" class="date-group">
      <div class="group-title">{{ group.name }}</div>
      <div 
        v-for="conv in group.conversations" 
        :key="conv.id" 
        class="conversation-item"
        @click="openConversation(conv.id)"
      >
        <div class="conv-title">{{ conv.title }}</div>
        <div class="conv-time">{{ conv.displayTime }}</div>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useChatStore } from '@/stores/chat'
import { groupConversationsByDate } from '@/utils/date'

const chatStore = useChatStore()

// 分组后的对话列表
const groupedConversations = computed(() => {
  // 先按时间倒序排序所有对话
  const sorted = [...chatStore.allConversations].sort(
    (a, b) => new Date(b.created_at).getTime() - new Date(a.created_at).getTime()
  )
  // 再按日期分组
  return groupConversationsByDate(sorted)
})

function openConversation(convId: string) {
  chatStore.loadConversation(convId)
  // 跳转到聊天页面
}
</script>

<style scoped>
.date-group {
  margin-bottom: 24px;
}

.group-title {
  font-size: 14px;
  font-weight: 500;
  color: #6b7280;
  margin-bottom: 12px;
  padding: 0 16px;
}

.conversation-item {
  padding: 12px 16px;
  border-radius: 8px;
  cursor: pointer;
  transition: background-color 0.2s;
}

.conversation-item:hover {
  background-color: #f3f4f6;
}

.conv-title {
  font-size: 15px;
  color: #111827;
  margin-bottom: 4px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.conv-time {
  font-size: 12px;
  color: #9ca3af;
}
</style>
```

---

## 四、最终效果对比
| 优化前 | 优化后 |
|--------|--------|
| 分组重叠，消息重复出现 | 分组互斥，一个消息只在一个分组 |
| 混乱的"今天/昨天/前天/本周/上周" | 清晰的"今天/昨天/近7天/本月/上月" |
| 日期显示不一致，需要手动换算 | 统一的显示格式，一目了然 |
| 排序混乱 | 严格按时间倒序排列 |

---

## 五、落地说明
1. **完全重写**：直接删掉你原来所有的日期分组代码，替换成上面的实现
2. **零依赖**：只使用原生JS日期API，不引入任何新依赖
3. **兼容旧数据**：自动处理所有ISO格式的日期字符串
4. **可扩展**：以后要修改分组规则，只需要调整`getDateGroup`函数即可

这个实现是微信、QQ、抖音等所有主流产品都在使用的标准日期分组逻辑，用户已经形成了使用习惯，不需要任何学习成本。