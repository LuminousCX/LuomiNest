# LuomiNest 模式系统与工具能力汇总

> 本文档汇总 LuomiNest 的三种对话模式及其对应的 AI 工具/上下文传递机制。

---

## 一、模式系统总览

| 模式 | 枚举值 | 前端标签 | 引擎 | 一句话概括 |
|------|--------|----------|------|-----------|
| **普通模式** | `normal` | 普通 | OpenAI Function Calling | 快速对话，仅 4 个定时任务工具 |
| **专业模式·标准** | `standard` | 标准 | 工作流引擎 (JSON Plan) | 平衡型，排除 29 个浏览器细粒度工具 |
| **专业模式·超长** | `ultra` | 超长 | 工作流引擎 (JSON Plan) | 全量工具，适合复杂长任务 |

> **关键设计原则**：三种模式**上下文完全隔离**。切换模式必须新建对话，不同模式的对话在数据库中通过 `chat_mode` 列标记和隔离。

---

## 二、普通模式 (NORMAL)

### 2.1 基本信息

| 属性 | 值 |
|------|----|
| 代码枚举 | `ChatMode.NORMAL` / `"normal"` |
| 工具路由 | `tool_registry`（OpenAI function calling 机制） |
| 是否启用工作流引擎 | **否** (`is_workflow=False`) |
| 适用场景 | 快速问答、简单对话、定时任务管理 |

### 2.2 工具白名单（4 个）

仅以下 4 个定时任务操作工具可通过 AI 调用：

| 工具名 | 用途 |
|--------|------|
| `create_scheduled_task` | 创建定时任务 |
| `list_scheduled_tasks` | 列出所有定时任务 |
| `get_scheduled_task` | 获取单个定时任务详情 |
| `delete_scheduled_task` | 删除定时任务 |

### 2.3 上下文传递

| 环节 | 说明 |
|------|------|
| System Prompt | 由 `context_service.build_system_prompt(agent_id)` 构建 |
| 记忆注入 | 通过 `context_service.inject_memory()` 自动注入主 Agent 记忆 |
| 工具过滤 | `ToolFilterMiddleware` 根据 `tool_whitelist` 过滤，AI 只能看到白名单内的 4 个工具 |
| 执行方式 | 标准的 OpenAI Function Calling：AI 返回 function_call，直接同步执行 |
| 响应方式 | SSE 流式返回文本 |

---

## 三、专业模式·标准 (STANDARD)

### 3.1 基本信息

| 属性 | 值 |
|------|----|
| 代码枚举 | `ChatMode.STANDARD` / `"standard"` |
| 工具路由 | `internal_tool_registry`（JSON 计划机制） |
| 是否启用工作流引擎 | **是** (`is_workflow=True`) |
| WorkflowMode 映射 | `WorkflowMode.STANDARD` |
| 适用场景 | 需要 AI 规划执行的复杂任务，但不需要细粒度浏览器控制 |

### 3.2 工作流参数

| 参数 | 值 |
|------|----|
| 最大迭代次数 (`max_iterations`) | 20 |
| 最大并发数 (`max_concurrent`) | 3 |
| 规划温度 (`planning_temperature`) | 0.3 |
| 综合温度 (`synthesis_temperature`) | 0.4 |
| 规划最大 Token (`planning_max_tokens`) | 8,192 |
| 跳过确认 (`skip_confirmation`) | `false`（用户需确认计划后执行） |

### 3.3 可用工具（排除 29 个浏览器细粒度工具后）

STANDARD 模式**排除**以下 29 个浏览器自动化工具，其余所有工具可用：

#### ❌ 被排除的浏览器工具（29 个）

**导航类（6 个）**：`browser.navigate`、`browser.go_back`、`browser.go_forward`、`browser.reload`、`browser.get_url`、`browser.get_page_title`

**交互类（9 个）**：`browser.click`、`browser.double_click`、`browser.right_click`、`browser.hover`、`browser.type`、`browser.clear_input`、`browser.press_key`、`browser.select_option`、`browser.scroll`

**提取类（5 个）**：`browser.get_dom_tree`、`browser.get_text`、`browser.get_attribute`、`browser.get_html`、`browser.screenshot`

**等待类（3 个）**：`browser.wait_for_load`、`browser.wait_for_element`、`browser.wait_for_url`

**执行类（2 个）**：`browser.execute_js`、`browser.get_history`

**标签页类（4 个）**：`browser.get_tabs`、`browser.switch_tab`、`browser.open_tab`、`browser.close_tab`

#### ✅ STANDARD 模式下可用的工具

| 模块 | 工具名 | 功能 |
|------|--------|------|
| **browser** | `browser.search` | 浏览器搜索（高层语义工具；构建搜索 URL 后导航，不属于细粒度工具） |
| **schedule** | `schedule.create` | 创建定时任务 |
| | `schedule.list` | 列出所有定时任务 |
| | `schedule.get` | 获取单个定时任务详情 |
| | `schedule.delete` | 删除定时任务 |
| **memory** | `memory.search` | 语义检索记忆中枢（向量召回） |
| | `memory.build_context` | 构建记忆上下文（按优先级注入档案/事实/知识/每日记录） |
| | `memory.list_facts` | 列出记忆事实 |
| | `memory.create_fact` | 创建新事实 |
| | `memory.update_fact` | 更新事实 |
| | `memory.delete_fact` | 删除事实 |
| | `memory.get_summary` | 获取 AI 摘要 |
| | `memory.update_summary` | 更新 AI 摘要 |
| | `memory.get_knowledge` | 获取知识库 |
| | `memory.get_profile` | 获取用户画像 |
| | `memory.distill` | 对话蒸馏为记忆 |
| | `memory.update_knowledge` | 更新知识库 |
| | `memory.update_profile` | 更新用户画像 |
| | `memory.append_daily` | 追加每日记录 |
| | `memory.get_daily` | 获取每日记录 |
| | `memory.list_dailies` | 列出有记录的日期 |
| | `memory.vector_rebuild` | 重建向量索引 |
| | `memory.promote_conversation_facts` | 对话级事实提升为 Agent 级 |
| | `memory.clear_facts` | 清空所有事实 |
| **console** | `console.execute` | 在控制台沙盒执行命令（白名单 + 超时保护） |
| **subagent** | `subagent.delegate` | 将任务委派给子 Agent 独立执行 |
| **smart_home** | `smart_home.control` | 控制智能家居设备 |
| | `smart_home.list_devices` | 列出所有设备 |
| | `smart_home.list_scenes` | 列出所有场景 |
| **market** | `market.install` | 安装扩展市场内容 |
| | `market.uninstall` | 卸载扩展市场内容 |
| | `market.list_installed` | 列出已安装内容 |
| | `market.get_leaderboard` | 获取市场排行榜 |
| **platform** | `platform.list_instances` | 列出所有平台实例 |
| | `platform.start_instance` | 启动平台实例 |
| | `platform.stop_instance` | 停止平台实例 |
| | `platform.send_message` | 通过平台实例发送消息 |

### 3.4 上下文传递

| 环节 | 说明 |
|------|------|
| System Prompt | 使用英文 `_WORKFLOW_SYSTEM_PROMPT` 模板，动态注入 `{available_tools}`（过滤后的工具列表） |
| 记忆注入 | `workflow_context_manager.inject_memory_context()` 自动注入 |
| 执行流程 | **分析** → **规划** → **等待确认** → **执行**（按依赖顺序） → **综合** |
| AI 交互格式 | AI 返回 JSON 格式的计划（含 analysis → plan → tasks），引擎解析后按依赖图执行 |
| 响应方式 | SSE 流式返回阶段事件 + 子任务执行事件 |

---

## 四、专业模式·超长 (ULTRA)

### 4.1 基本信息

| 属性 | 值 |
|------|----|
| 代码枚举 | `ChatMode.ULTRA` / `"ultra"` |
| 工具路由 | `internal_tool_registry`（JSON 计划机制） |
| 是否启用工作流引擎 | **是** (`is_workflow=True`) |
| WorkflowMode 映射 | `WorkflowMode.ULTRA` |
| 适用场景 | 复杂长任务、需要浏览器自动化控制、大规模数据处理 |

### 4.2 工作流参数

| 参数 | 值 |
|------|----|
| 最大迭代次数 (`max_iterations`) | 100 |
| 最大并发数 (`max_concurrent`) | 8 |
| 规划温度 (`planning_temperature`) | 0.5 |
| 综合温度 (`synthesis_temperature`) | 0.6 |
| 规划最大 Token (`planning_max_tokens`) | 16,384 |
| 跳过确认 (`skip_confirmation`) | `false`（用户需确认计划后执行） |

### 4.3 可用工具（全量）

ULTRA 模式**不排除任何工具**，AI 可以看到所有注册到 `internal_tool_registry` 的工具。除 STANDARD 模式的所有工具外，额外包含以下 29 个浏览器细粒度工具：

#### 额外可用的浏览器工具（29 个）

| 分类 | 工具列表 |
|------|---------|
| **导航** | `browser.navigate`、`browser.go_back`、`browser.go_forward`、`browser.reload`、`browser.get_url`、`browser.get_page_title` |
| **交互** | `browser.click`、`browser.double_click`、`browser.right_click`、`browser.hover`、`browser.type`、`browser.clear_input`、`browser.press_key`、`browser.select_option`、`browser.scroll` |
| **提取** | `browser.get_dom_tree`、`browser.get_text`、`browser.get_attribute`、`browser.get_html`、`browser.screenshot` |
| **等待** | `browser.wait_for_load`、`browser.wait_for_element`、`browser.wait_for_url` |
| **执行** | `browser.execute_js`、`browser.get_history` |
| **标签页** | `browser.get_tabs`、`browser.switch_tab`、`browser.open_tab`、`browser.close_tab` |

#### 全量工具总数

> 共约 **50+ 个工具**，涵盖 browser（30 个，含 `browser.search`）、schedule（4 个）、memory（18 个）、console（1 个）、subagent（1 个）、smart_home（3 个）、market（4 个）、platform（4 个）八大模块。

### 4.4 上下文传递

| 环节 | 说明 |
|------|------|
| System Prompt | 同 STANDARD，使用 `_WORKFLOW_SYSTEM_PROMPT` 模板，但注入全量工具列表 |
| 记忆注入 | 同 STANDARD，`workflow_context_manager.inject_memory_context()` |
| 执行流程 | 同 STANDARD，**分析 → 规划 → 等待确认 → 执行 → 综合** |
| AI 交互格式 | 同 STANDARD，JSON 计划格式 |
| 响应方式 | 同 STANDARD，SSE 流式 |

---

## 五、三模式横向对比

### 5.1 核心参数对比

| 对比项 | 普通 (NORMAL) | 专业·标准 (STANDARD) | 专业·超长 (ULTRA) |
|--------|:------------:|:-------------------:|:----------------:|
| 工具总数 | 4 个 | ~25 个 | ~50+ 个 |
| 浏览器细粒度工具 | ❌ | ❌ 排除 | ✅ 全量 |
| 浏览器搜索 (`browser.search`) | ❌ | ✅ | ✅ |
| 定时任务 | ✅ | ✅ | ✅ |
| 记忆中枢 | ❌ | ✅（18 个工具） | ✅（18 个工具） |
| 控制台执行 | ❌ | ✅ | ✅ |
| 子 Agent 委派 | ❌ | ✅ | ✅ |
| 智能家居 | ❌ | ✅ | ✅ |
| 扩展市场 | ❌ | ✅ | ✅ |
| 平台接入 | ❌ | ✅ | ✅ |
| 工作流引擎 | ❌ | ✅ | ✅ |
| 最大迭代次数 | N/A | 20 | 100 |
| 最大并发 | N/A | 3 | 8 |
| 规划温度 | N/A | 0.3 | 0.5 |
| 规划最大 Token | N/A | 8,192 | 16,384 |

### 5.2 流程对比

```
普通模式 (NORMAL):
  用户输入 → [Function Calling] → AI 调用白名单工具 → 流式返回文本

专业模式·标准 (STANDARD):
  用户输入 → [工作流引擎] → 分析 → AI 规划(JSON) → 等待确认 → 按依赖顺序执行子任务(最多20轮/3并发) → 综合结果 → 流式返回

专业模式·超长 (ULTRA):
  用户输入 → [工作流引擎] → 分析 → AI 规划(JSON) → 等待确认 → 按依赖顺序执行子任务(最多100轮/8并发) → 综合结果 → 流式返回
```

### 5.3 数据持久化

| 对话属性 | 说明 |
|---------|------|
| 数据库表 | `conversations` |
| 模式标记列 | `chat_mode`（VARCHAR(32)，默认 `"normal"`） |
| 创建 API | `POST /chat/conversations`，body 中传入 `chat_mode` |
| 前端同步 | 从 `chatStore.currentConversation.chat_mode` 同步当前模式 |

---

## 六、关键源码文件

| 文件 | 说明 |
|------|------|
| `backend/app/core/chat_mode.py` | 核心模式定义、工具配置、辅助函数 |
| `backend/app/core/workflow/models.py` | 工作流模式参数配置、会话模型 |
| `backend/app/core/workflow/engine.py` | 工作流引擎（模式判断、工具过滤） |
| `backend/app/core/workflow/internal_registry.py` | 内部工具注册表 |
| `backend/app/core/workflow/register_tools.py` | 注册所有内部工具（~50+ 个） |
| `backend/app/services/chat_service.py` | 对话服务（普通模式分流、白名单注入） |
| `backend/app/core/tools/builtin/browser_automation.py` | 29 个浏览器细粒度工具定义 |
| `backend/app/core/tools/builtin/scheduler_tool.py` | 4 个定时任务工具定义 |
| `frontend/src/renderer/src/types/workflow.ts` | 前端类型定义（ChatMode 等） |
| `frontend/src/renderer/src/composables/useWorkbenchMessages.ts` | 前端模式切换核心逻辑 |
| `frontend/src/renderer/src/stores/workflow.ts` | 前端工作流 store |
| `frontend/src/renderer/src/stores/chat.ts` | 前端对话 store |
