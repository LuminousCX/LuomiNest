Verify each finding against the current code and only fix it if needed.

Inline comments:
In `@frontend/src/renderer/src/utils/marketplace-icons.ts`:
- Around line 2-14: ITEM_ICON_MAP is missing several icon entries referenced by
marketplace-data (so those keys fall back to DEFAULT_ICON); update ITEM_ICON_MAP
in marketplace-icons.ts to include the missing lucide-vue-next components (e.g.,
LayoutGrid, Cpu, Wrench, Puzzle, MessageCircle, Code, Image, Heart and any other
names found in frontend/src/renderer/src/config/marketplace-data.ts) by
importing them from 'lucide-vue-next' and adding them to the ITEM_ICON_MAP
object so lookup by key returns the correct Component instead of DEFAULT_ICON.

In `@frontend/src/renderer/src/views/PluginMarketView.vue`:
- Around line 37-40: resetFilters 目前只清除了 category 和搜索词（store.setFilter('plugin',
{ category: undefined }) / store.clearSearch()），未恢复其他可能的插件筛选状态；请改为调用 store
提供的完整重置方法（例如 store.resetFilters('plugin') 或 store.reset('plugin')，根据 store API
确切名称使用对应方法）以重置该插件视图的所有筛选项，替换掉当前的 setFilter/clearSearch 调用并确保所有 plugin
相关的筛选状态都被恢复为默认值。

In `@frontend/src/renderer/src/views/SkillMarketView.vue`:
- Around line 37-40: The resetFilters function only clears category and search
(it calls store.setFilter('skill', { category: undefined }) and
store.clearSearch()), leaving other MarketplaceFilters (rating/price/tags/etc.)
intact; replace this ad-hoc logic with the store's unified reset entry (e.g.,
call store.resetFilters() or store.clearAllFilters()) so all MarketplaceFilters
state is cleared, or if that helper does not exist add a single store method
(resetFilters/resetAllFilters) that resets every filter field used by
MarketplaceFilters and invoke it from resetFilters.

In `@frontend/src/renderer/src/views/TasksView.vue`:
- Around line 398-400: The month-navigation code mutates Date d using
d.setMonth(...) then d.setDate(1), which can overflow when d is at a month-end
(e.g., 2026-01-31) and skip months; fix by normalizing the day to the 1st before
changing the month or by replacing the logic with constructing a new Date for
the target month (e.g., new Date(d.getFullYear(), d.getMonth() ± 1, 1)) so month
increments/decrements never carry over day overflow; apply the same change to
the symmetric block handling the other direction (the second occurrence around
the subsequent setMonth/setDate pair).建议添加范围2000-2099年范围
- Around line 455-458: The week-view click handler currently calls
openCreateModal(prefillDate) but only sets newTask.value.dueDate, so the clicked
time slot isn't prefilled; change openCreateModal to accept a second optional
parameter (e.g., prefillTimeSlot?: string) and inside the function assign
newTask.value.timeSlot = prefillTimeSlot when provided (preserving default
otherwise). Update all callers (the week-view click handler and the other
occurrence around the 811–815 region) to pass the clicked slot identifier/string
along with day.fullDate so the modal opens with both dueDate and timeSlot
prefilled.
- Around line 803-815: The week/month views use non-focusable divs with `@click`
(the week-cell elements generated from weekDays/timeSlots calling
openCreateModal) so keyboard users can't activate them; update these interactive
divs to be real controls or make them keyboard-accessible: either replace the
div.week-cell (and the analogous month view cell) with <button type="button">
elements that call openCreateModal(day.fullDate), or add tabindex="0",
role="button" and a keydown handler that triggers openCreateModal when Enter or
Space is pressed; ensure the same change is applied to the other similar block
(month view) so both week and month entry points are reachable via keyboard.
- Around line 145-146: The code assigns data.tasks directly to tasks.value which
lets poisoned or legacy objects break downstream code (filteredTasks / template
where t.title.toLowerCase(), t.desc.toLowerCase(), t.tags.some(...) are used);
add a validation/normalization step before assigning: verify data.tasks is an
array, map each entry through a normalizeTask routine that ensures title and
desc are strings (fallback to ""), tags is an array (fallback to []), and other
new fields have safe defaults, catching any exceptions and falling back to an
empty array or your canonical default tasks; replace the direct assignment to
tasks.value with the normalized array so filteredTasks and template access safe,
well-shaped items.