Verify each finding against the current code and only fix it if needed.

In `@frontend/src/renderer/src/views/MarketView.vue` around lines 69 - 73,
switchTab currently updates only local state (activeTab, store.setFilter,
showFilters) but does not sync the route query, causing stale route.query.tab
and navigation/bookmark issues; update switchTab to call router.replace() with
the same route but with query.tab set to the new tab value (preserving other
query params if any) so route.query.tab matches activeTab, and keep the existing
store.setFilter and showFilters behavior; reference the switchTab function and
use router.replace() and route.query.tab to implement this synchronization.

Verify each finding against the current code and only fix it if needed.

In `@frontend/src/renderer/src/stores/chat.ts` around lines 91 - 139, In
loadConversation, prevent stale responses from overwriting the store by
capturing the current activeAgentId.value and convId into local constants before
the await apiGet(...) call and, after the await (and in the catch/finally paths
if needed), verify that activeAgentId.value and the current convId still match
those captured values; if they don't match, abort updating
agentCurrentConversation, agentMessages, and conversationMessagesCache.
Alternatively implement a per-agent/per-request sequence id or AbortController
and check the sequence/aborted state before applying any state mutations;
reference loadConversation, activeAgentId, convId, agentCurrentConversation,
agentMessages, and conversationMessagesCache when making the checks.

Verify each finding against the current code and only fix it if needed.

In `@frontend/src/renderer/src/stores/chat.ts` around lines 28 - 29, The single
global boolean isLoadingConversation causes race conditions for parallel loads;
change isLoadingConversation from a ref<boolean> to a ref<Record<string,
boolean>> keyed by agentId or conversationId (similar to
conversationMessagesCache) and update all reads/writes to use that key (set
isLoadingConversation.value[agentId]=true/false and check existence for loading
state). Locate and update every place that accesses isLoadingConversation
(including the loading setters/getters around the message-fetching logic and the
areas noted in the diff) to read/write the per-agent key and ensure cleanup
deletes the key when finished to avoid stale entries.

Verify each finding against the current code and only fix it if needed.

In `@frontend/src/main/index.ts` around lines 833 - 839, The handler decodes
url.pathname into relativePath then passes it into resolveModelFile which
decodes it again, enabling double-decoding path traversal bypass; fix by
removing one decode: either stop calling decodeURIComponent when constructing
relativePath in the request handler (keep raw url.pathname) or remove the second
decode inside resolveModelFile so only a single decode happens; update
references to relativePath and the resolveModelFile call to reflect the
single-decoding choice and add a comment near resolveModelFile and the handler
to document the decoding responsibility.



Verify each finding against the current code and only fix it if needed.

In `@frontend/installer.iss` around lines 336 - 339, The message for an
already-installed detection is hardcoded in English; add a localized message key
(e.g., AlreadyInstalledMsg) to your [CustomMessages] for both chinesesimplified
and english, then replace the inline English text in the MsgBox call with
FmtMessage(CustomMessage('AlreadyInstalledMsg'), []) (keeping the existing
FmtMessage(CustomMessage('WelcomeDesc1'), []) and concatenation/line breaks), so
MsgBox uses CustomMessage('AlreadyInstalledMsg') instead of the hardcoded
'{`#MyAppName`} is already installed...'.

Verify each finding against the current code and only fix it if needed.

In `@frontend/installer.iss` around lines 315 - 320, The code currently calls
WizardForm.TasksList.Items.IndexOfName which doesn't exist and also assumes
tasks are at fixed indexes (but a group header shifts indexes); replace those
IndexOfName calls by locating tasks properly—either use
WizardForm.TasksList.Items.IndexOf(CustomMessage('cm:ShortcutsDesktop')) /
Items.IndexOf(CustomMessage('cm:ShortcutsStartMenu')) /
Items.IndexOf(CustomMessage('cm:ShortcutsAutoLaunch')) to get the correct item
index before setting WizardForm.TasksList.Checked, or (preferably) use the
TasksList helper APIs: call WizardForm.TasksList.IsTaskSelected('desktopicon') /
IsTaskSelected('startmenu') / IsTaskSelected('autolaunch') to read state and
call the corresponding selector (e.g. SetTaskSelected or the Tasks API your
codebase provides) to set selection based on DesktopCheck.Checked,
StartMenuCheck.Checked and AutoLaunchCheck.Checked; update the block under
CurPageID = ShortcutsPage.ID to use these valid methods instead of IndexOfName.

Verify each finding against the current code and only fix it if needed.

In `@backend/build.sh` around lines 97 - 101, 更新提示中已过时的构建命令：修改 backend/build.sh
中打印的“cd ../frontend && ./build-installer.sh (or .ps1 on Windows)”那一行，将引用的
./build-installer.sh 替换为 ../build-all.sh（或直接删除这两行只保留已有的 ../build-all.sh
提示），以避免提示用户运行不存在的脚本；定位参考点为文件中输出“Next steps:”后紧接的 echo 行并相应更新文本。