_FACT_EXTRACT_PROMPT = """你是一个记忆提取助手。从用户消息中提取关键信息，包括用户名字和事实。

规则：
1. profile_name：只提取用户自己的名字，不提取别人的名字
2. 如果用户在问问题（"我叫什么？"）或假设性语句（"如果我叫小明"），profile_name留空
3. 名字长度不超过20个字符
4. 事实提取规则：
   - 提取所有明确陈述、强暗示的信息，包括用户提到的多个选项或备选方案
   - 如果用户提到多个可能的选项（如"意大利也行"、"美国也可以"），每个选项都作为独立事实提取
   - 每条事实必须有明确的类别
   - 置信度：0.9-1.0（明确陈述）、0.7-0.8（强暗示）、0.5-0.6（推断模式）
   - 如果是纠正之前的信息，使用 correction 类别，并在 source_error 中记录之前的错误信息

类别：
- preference: 用户偏好（喜欢/不喜欢什么）
- knowledge: 用户知识/专长
- context: 用户当前背景（工作、项目等）
- behavior: 用户行为模式（习惯、风格等）
- goal: 用户目标/计划（如计划去的地方、要做的事情）
- correction: 纠正之前的错误信息

过期时间规则（expires_at）：
- 临时性事实必须设置过期时间：明天/下周/月底等事件后应过期，用相对描述如"2025-01-15"或"2025-01-31"
- 长期稳定的事实（如职业、技能、永久偏好）不需要设置过期时间，expires_at 留空
- 典型需要过期时间的情景：
  * "我明天有考试" → expires_at 设为明天日期
  * "下周要去旅游" → expires_at 设为下周某天
  * "这个月在学习 Python" → expires_at 设为月底
  * 临时项目/任务 → expires_at 设为任务完成日期或1个月后
- 典型不需要过期时间的情景：
  * "我是软件工程师"（职业）
  * "我喜欢 Vim"（长期偏好）
  * "我会 Python 和 Java"（技能）

请严格按以下JSON格式回复，不要添加任何其他内容：
{{"profile_name": "用户名字或空字符串", "facts": [{{"content": "事实内容", "category": "类别", "confidence": 0.9, "source_error": "", "expires_at": "ISO日期字符串或空字符串"}}]}}

如果消息中不包含可提取的内容，返回：
{{"profile_name": "", "facts": []}}

用户消息：{message}"""

_CORRECTION_HINT = "特别注意：用户在最近的对话中表达了纠正/不满，请以 correction 类别、confidence >= 0.95 记录正确做法，并在 source_error 中记录之前的错误信息。"

_REINFORCEMENT_HINT = "特别注意：用户在最近的对话中确认了某个信息，请以 preference 或 behavior 类别、confidence >= 0.9 记录确认的做法。"

_DISTILL_PROMPT = """你是一个记忆蒸馏助手。根据当前记忆和近期对话，完成两项任务：

任务1：提取结构化事实（回填到用户档案）
从对话中提取所有可确认的事实信息，包括用户名字、偏好、背景、目标等。特别注意：如果用户提到多个选项或备选方案（如"意大利也行"、"美国也可以"），每个选项都作为独立事实提取。

任务2：更新叙事性总结
保留已有的正确信息，只更新或补充。每个部分用简洁的要点列出，不要写长段落。事件时间线按时间倒序排列，最多保留20条。

规则：
1. 事实提取是最高优先级——如果对话中用户说了名字，必须提取
2. 如果用户纠正了之前的信息，用 correction 类别记录，source_error 填写被纠正的旧信息
3. 名字提取规则：只提取用户自己的名字，不提取别人或假设性的名字
4. 总结中的信息必须与提取的事实一致，不能矛盾
5. 如果用户提到多个选项或备选方案，所有选项都应在总结中体现
6. 维度划分规则：
   - 用户画像：客观身份事实（姓名、职业、年龄段、地区、技术栈等）
   - 偏好设置：交互行为偏好（回复风格、代码风格、是否希望被称呼名字等）
   - 兴趣目标：学习/生活兴趣（想学的技术、想去的地方、目标计划等，包括多个备选选项）
   - 近期状态：临时状态（当前心情、本周状态等）
   - 事件时间线：重要事件、里程碑

请严格按以下JSON格式回复，不要添加任何其他内容：
{{
  "facts": [{"content": "事实内容", "category": "类别", "confidence": 0.9, "source_error": "", "expires_at": "ISO日期或空字符串", "supersedes": "被替代的旧事实内容或空字符串"}],
  "profile_name": "",
  "static_facts": ["稳定偏好1", "稳定偏好2"],
  "dynamic_context": ["当前状态1", "当前状态2"],
  "summary": {{
    "用户画像": "",
    "偏好设置": "",
    "兴趣目标": "",
    "近期状态": "",
    "事件时间线": ""
  }}
}}

其中 profile_name 仅在对话中明确提到用户名字时填写，否则留空。
static_facts: 长期稳定的事实（职业、技能、永久偏好），留空或最多3条。
dynamic_context: 近期正在做的事情或短期状态，留空或最多3条。
expires_at: 临时性事实设置过期时间，长期事实留空。
supersedes: 如果新事实替代了已有事实（如用户换了工作、搬了家、改变了偏好），填写被替代的旧事实的内容关键词，系统会自动降低旧事实的置信度。如果新事实不替代任何旧事实，留空。
summary 的每个字段用 Markdown 要点格式填写（每行以 "- " 开头），如果某个部分没有新信息则保留原文。

当前记忆：
- 用户名字：{current_name}
- 已有事实：{current_facts}
- 当前总结：
{current_summary}

近期对话摘要：
{conversation_summary}

{correction_hint}"""

_CORRECTION_PATTERNS_ZH = [
    "不对", "你理解错了", "你理解有误", "不是这样的", "错了",
    "重试", "重新来", "换一种", "改用", "别这样",
]

_CORRECTION_PATTERNS_EN = [
    "that's wrong", "you misunderstood", "try again", "redo",
    "not what i meant", "incorrect",
]

_REINFORCEMENT_PATTERNS_ZH = [
    "对，就是这样", "完全正确", "正是我想要的", "继续保持", "很好",
    "没错", "对的", "就是这样",
]

_REINFORCEMENT_PATTERNS_EN = [
    "yes exactly", "perfect", "that's right", "keep doing that",
    "this is great", "correct",
]

_MERGE_SUMMARY_PROMPT = """合并为一份统一的用户画像摘要：

<已有摘要>
{old_summary}
</已有摘要>

<新观察>
{new_summary}
</新观察>

要求：
1. 合并所有信息，去重去冗余
2. 新信息覆盖旧信息中的矛盾/过期内容
3. 必须保留五个部分的标题格式（## 标题），不能省略任何标题
4. 每个部分使用 bullet point（每条以 "- " 开头），每条 ≤30字
5. 没有相关信息的部分保留标题，内容留空
6. 总长度 ≤500字

五个部分（必须保留）：
## 用户画像
## 偏好设置
## 兴趣目标
## 近期状态
## 事件时间线"""

_DISTILL_PROMPT_ROUND = """你是对话分析专家。从对话中提取关键信息并整理成五个部分的摘要。

<对话>
{messages}
</对话>

要求：
1. 将信息分类到五个部分中
2. 每个部分使用标题（## 标题）开头
3. 内容使用中文 bullet point（每条以 "- " 开头），每条 ≤30字
4. 忽略寒暄和纯确认性回复
5. 没有相关信息的部分留空
6. 总长度 ≤300字

五个部分：
## 用户画像
## 偏好设置
## 兴趣目标
## 近期状态
## 事件时间线"""

_SUMMARY_EXTRACT_PROMPT = """你是一个摘要解析助手。从用户提供的Markdown格式摘要中提取五个部分的内容。

规则：
1. 只提取与每个部分相关的内容
2. 如果某个部分没有内容，返回空字符串
3. 保持原有格式，使用Markdown要点格式（每行以 "- " 开头）
4. 不要添加任何额外内容

五个部分：
- 用户画像：客观身份事实（姓名、职业、年龄段、地区、技术栈等）
- 偏好设置：交互行为偏好（回复风格、代码风格、是否希望被称呼名字等）
- 兴趣目标：学习/生活兴趣（想学的技术、想去的地方、目标计划等）
- 近期状态：临时状态（当前心情、本周状态等）
- 事件时间线：重要事件、里程碑

请严格按以下JSON格式回复，不要添加任何其他内容：
{{
  "用户画像": "",
  "偏好设置": "",
  "兴趣目标": "",
  "近期状态": "",
  "事件时间线": ""
}}

输入摘要：
{content}"""

_KNOWLEDGE_EXTRACT_PROMPT = """你是一个知识提取助手。从对话中提取有用的知识点、事实、概念、公式等。

规则：
1. 只提取有价值的、可复用的知识信息
2. 每个知识主题作为一个独立的节（section）
3. 每个节使用标题（## 标题）开头
4. 知识内容使用Markdown要点格式（每行以 "- " 开头）
5. 忽略寒暄、对话流程性内容
6. 合并相同主题的知识
7. 保持简洁，每条知识不超过30字

请按以下格式输出：

## 主题1
- 知识点1
- 知识点2

## 主题2
- 知识点3

输入对话：
{conversation}"""
