# =============================================================================
# LuomiNest Backend 环境变量示例
# =============================================================================
# 后端服务专用配置。复制本文件为 .env 并按需修改。
# 完整项目级配置见根目录 .env.example
# =============================================================================

# ── 应用核心 ──────────────────────────────────────────────────────────────────
APP_NAME=LuomiNest
DEBUG=False
ENVIRONMENT=production

# ── 认证与密钥 ────────────────────────────────────────────────────────────────
# 留空则自动生成并持久化到 data/config/ 目录
# LUOMINEST_AUTH_TOKEN=
# SECRET_KEY=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ── 数据库 ────────────────────────────────────────────────────────────────────
# 留空时自动使用 SQLite：sqlite+aiosqlite:///<DATA_DIR>/luominest.db
# DATABASE_URL=
REDIS_URL=redis://localhost:6379/0

# ── CORS ──────────────────────────────────────────────────────────────────────
CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","http://127.0.0.1:5173","http://127.0.0.1:3000"]

# ── LLM 供应商 ───────────────────────────────────────────────────────────────
LLM_DEFAULT_PROVIDER=openai
LLM_FALLBACK_CHAIN=openai,ollama
LLM_DEFAULT_MODEL=
LLM_DEFAULT_TEMPERATURE=0.7
LLM_DEFAULT_MAX_TOKENS=4096
LLM_DEFAULT_TOP_P=0.9
LLM_MAX_CONCURRENT_REQUESTS=3

OPENAI_API_KEY=
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
OLLAMA_BASE_URL=http://localhost:11434

# ── MQTT ──────────────────────────────────────────────────────────────────────
MQTT_BROKER_HOST=localhost
MQTT_BROKER_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=

# ── 数据目录 ──────────────────────────────────────────────────────────────────
DATA_DIR=./data
UPLOAD_DIR=./data/uploads
AVATAR_DIR=./data/avatars
PLUGIN_DIR=./plugins
SKILL_DIR=./skills
LIVE2D_MODEL_PATH=./models/live2d
VRM_MODEL_PATH=./models/vrm

FILE_MAX_SIZE=104857600
TTS_PROXY=

# ── Agent 集群 ────────────────────────────────────────────────────────────────
APP_SELF_BASE_URL=http://localhost:8000
A2A_SERVERS=[]
A2A_TIMEOUT_SECONDS=60
A2A_MAX_DEPTH=3

# ── 可选集成 ──────────────────────────────────────────────────────────────────
GITHUB_TOKEN=
EXTERNAL_PARSE_API_URL=
