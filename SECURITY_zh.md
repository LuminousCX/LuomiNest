# 安全策略 (Security Policy)

[English](SECURITY.md) | [简体中文](SECURITY_zh.md) | [日本語](SECURITY_ja.md)

LuomiNest 团队高度重视软件及用户数据的安全性与隐私保护。本政策明确了受支持的版本范围、安全漏洞的提报渠道以及系统内置的安全基准实践。

---

## 受支持的版本

安全补丁会积极合入主开发分支（`master`），并按需为受支持的版本发布小版本修复更新。

| 版本分支 | 安全更新支持 | 维护状态 |
| ------- | --------- | ------ |
| `master` 分支 | ✅ 支持 | 主开发活跃分支 |
| `0.8.x` | ✅ 支持 | 当前稳定发布分支 |
| `0.7.x` | ⚠️ 有限支持 | 仅针对严重等级以上的安全问题进行维护 |
| `< 0.7.0` | ❌ 不再支持 | 生命周期结束（强烈建议升级） |

---

## 报告安全漏洞

**请切勿在公开的 GitHub Issues 或公开 Pull Request 中报告安全漏洞！**

如果你在 LuomiNest 中发现了潜在的安全漏洞或安全隐患，请通过私密邮件直接联系团队：

- **联系邮箱**：[luminouschenxi@outlook.com](mailto:luminouschenxi@outlook.com)
- **邮件主题**：`[SECURITY] LuomiNest 漏洞报告`

### 报告应包含的信息
为了协助我们快速定位并修复漏洞，请在邮件中尽可能提供以下内容：
1. **漏洞描述**：漏洞的详细类型、触发机理及潜在的危害评估；
2. **复现步骤**：清晰的操作步骤或最小化 PoC（概念验证代码/请求）；
3. **受影响版本**：发现该问题的具体版本号、Commit Hash 或运行环境（Windows / Linux / macOS / Docker）；
4. **修复建议**：若你已有修复思路或补丁代码，欢迎随信附上。

### 响应时效与协同披露 (Coordinated Disclosure)
- **初步确认时效**：我们承诺在收到邮件后的 **72 小时** 内进行初步评估与确认；
- **排查与修复**：团队将在私有环境中评估严重级别并制作修复补丁，期间会定期通过邮件同步进度；
- **负责任的协同披露**：我们遵循[负责任的漏洞披露原则](https://en.wikipedia.org/wiki/Coordinated_vulnerability_disclosure)。在官方补丁发布前，恳请报告者对漏洞细节严格保密。补丁发布后，我们将在 Release 说明中对报告者致谢（除非报告者希望匿名）。

---

## 安全架构与内置防护机制

LuomiNest 在架构设计上针对多用户 Agent 与本地自主执行环境构建了多层安全防护屏障：

1. **鉴权机制与令牌治理**：
   - 针对 REST API 与 WebSocket 采用 JWT 及本地内部 Token 双模式鉴权；
   - 细粒度 RBAC 权限控制与路由级依赖注入守卫。
2. **本地环境隔离与命令沙箱**：
   - 非受信系统命令执行受到 Command Guard 命令守卫与白名单策略的严格约束；
   - 本地沙箱隔离机制，防止恶意工具篡改主机关键配置或执行破坏性系统命令。
3. **提示词防注入与安全过滤**：
   - 面向用户输入及第三方平台消息集成提示词注入防御中间件，拦截越狱与提示词泄露攻击。
4. **数据隐私与本地加密**：
   - 会话历史、群友画像及记忆向量库 100% 本地化存储于单文件 SQLite（WAL 模式）；
   - 大模型 Provider API Key 及敏感环境变量采用 AES-256 本地加密保存。
5. **频率限制与防滥用机制**：
   - 关键认证端点与高频推理接口集成 SlowAPI 速率限制中间件。
6. **诊断日志脱敏与隐私保护**：
   - 导出与手动上传的诊断日志在前端与后端双重脱敏，自动剔除令牌、密钥与媒体附件，绝不私自上传用户本地隐私数据。

---

## 运维与开发安全最佳实践

- **机密凭证管理**：切勿将包含真实密钥的 `.env` 文件提交至代码仓库，请使用 `.env.example` 作为模版，并严格通过环境变量或配置界面录入。
- **网络通信加密**：对外提供公网访问时，Web 服务必须配置 TLS/HTTPS，WebSocket 必须配置 WSS，MQTT 设备通信必须启用 TLS 加密端口。
- **沙箱与守卫开启**：在生产与日常使用中，切勿擅自关闭 Agent 的命令守卫与安全沙箱。

---

## 安全审计与合规

项目持续推进以下安全审计实践：
- 代码合并前进行自动化安全与代码质量审查（CodeRabbit）；
- 第三方依赖库安全漏洞周期性扫描；
- 自动化端到端鉴权防线回归测试。

感谢广大社区贡献者与安全研究人员共同守护 LuomiNest 的生态安全！
