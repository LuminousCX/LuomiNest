# 陪伴型 AI 自研手册 —— 从零开始训练属于你的 AI 伙伴

> **版本**:v0.1(初稿,持续补充中)
> **日期**:2026-09-19
> **定位**:个人学习 + 项目实施参考手册,覆盖"大模型基础概念 → 模型选型 → 微调训练 → 数据工程 → 语音形象 → 部署运维 → 长期演进"全链路
> **目标**:造一个"人味优先"的陪伴型 AI(日常陪伴 / 陪伴学习 / 游戏陪玩 / 直播娱乐),真人语气、长期记忆、可自训、可本地部署

---

## 目录

- [第 0 章 开始之前:总览与路线图](#第-0-章-开始之前总览与路线图)
  - 0.1 这本手册给谁看 / 0.2 我们要造的东西 / 0.3 总路线图 / 0.4 预算总账 / 0.5 十条核心结论
- [第 1 章 大模型基础:把名词一次讲透](#第-1-章-大模型基础把名词一次讲透)
  - 1.1 大模型的一生(训练流水线)/ 1.2 术语大全 / 1.3 模型文件版本详解 / 1.4 量化详解 / 1.5 参数量与 MoE / 1.6 上下文与 Token / 1.7 采样参数
- [第 2 章 模型选型:2025.6 ~ 2026.9 开源模型全景清单](#第-2-章-模型选型20256--20269-开源模型全景清单)
- [第 3 章 微调技术详解:LoRA / QLoRA / DPO](#第-3-章-微调技术详解lora--qlora--dpo)
- [第 4 章 数据工程:数据集 / 聊天记录导出 / 蒸馏 / 精修](#第-4-章-数据工程数据集--聊天记录导出--蒸馏--精修)
- [第 5 章 语音与形象:让她能听会说](#第-5-章-语音与形象让她能听会说)
- [第 6 章 RAG、记忆与 Agent:不训练也能变强](#第-6-章-rag记忆与-agent不训练也能变强)
- [第 7 章 硬件与算力:本地优先 + 云端租卡](#第-7-章-硬件与算力本地优先--云端租卡)
- [第 8 章 部署与日常运行](#第-8-章-部署与日常运行)
- [第 9 章 长期维护:让她跟着时代成长](#第-9-章-长期维护让她跟着时代成长)
- [第 10 章 架构会过时吗:五年视角的技术判断](#第-10-章-架构会过时吗五年视角的技术判断)
- [附录 A 命令与参数速查表](#附录-a-命令与参数速查表)
- [附录 B 30 天行动日历](#附录-b-30-天行动日历)
- [附录 C 学习资源清单](#附录-c-学习资源清单)

---

# 第 0 章 开始之前:总览与路线图

## 0.1 这本手册给谁看

给一个这样的你:

- 有一台台式机(4070 Ti Super 16GB 显存)和一台笔记本(4070 8GB 显存),打算再添一张 24G 显卡
- 会写代码,但机器学习/深度学习是新手,被 LoRA、QLoRA、INT8、INT4、RLHF 这些名词轰炸过
- 预算有限:训练靠租卡(按量付费),长期部署靠本地(只付电费)
- 想要的不是"一个能干活的 AI",而是"一个像人的 AI"——陪伴、聊天、陪玩游戏、能直播,人味优先
- 明确不追求:跑分最强、编码最强、刷榜。那些是别人卷的方向,你卷的是"她像不像真人"

这本手册就是把你从"名词都看不懂"带到"能独立完成一轮微调并把 AI 部署到本地"的完整路径。

## 0.2 我们要造的东西

一句话描述:**一个跑在你自己显卡上的、有固定人格的、能记住你的、能听能说的、能陪你打游戏的 AI 伙伴。**

拆开是六个部件,每个部件用不同的技术解决:

| 部件 | 负责什么 | 用什么技术 | 要不要训练 |
|---|---|---|---|
| 大脑 | 理解与生成语言 | 开源大模型(26B~36B 级) | 买现成的 |
| 人格 | 语气、性格、口头禅、"她是谁" | **LoRA/QLoRA 微调** | ✅ 这是你唯一要训的 |
| 知识 | 具体资料、你的文档、实时信息 | RAG(检索增强生成) | ❌ 不训练 |
| 能力 | 查资料、算东西、操作软件、玩游戏 | Agent + 工具调用 | ❌ 不训练 |
| 记忆 | 记住你聊过的事、你的偏好 | 记忆系统(向量库+摘要) | ❌ 不训练 |
| 声音 | 说话像真人 | 开源 TTS + 音色克隆 | ❌ 不训练(少量微调可选) |

**最重要的认知**:新手最容易犯的错是"把所有问题都指望训练解决"。实际上 6 个部件里只有"人格"值得你训练,其余全部用现成方案组装。训练只负责"性格和语气",因为这是系统提示词怎么写都装不出来的东西;而知识、能力、记忆用训练去解决是又贵又差的下下策。

## 0.3 总路线图

```
阶段 0(本周, 0元)      本地跑通"不训练的陪伴AI"
   │                    Ollama + 26B/30B模型 + 人设提示词 + SillyTavern
   │                    目的:体验产品形态,建立直觉
   ▼
阶段 1(第2-3周, 0元)    加上"耳朵嘴巴和记性"
   │                    记忆系统(Mem0) + RAG + TTS(CosyVoice2)
   │                    目的:完整骨架成型,开始日常使用攒语料
   ▼
阶段 2(第4周, ~50元)    学会微调(本地冒烟)
   │                    Unsloth + Qwen3-14B + 500条测试数据 + 4070TiS
   │                    目的:把训练流水线跑通,格式对不对一眼可见
   ▼
阶段 3(第5-6周, ~200元) 正式训练(云端生产)
   │                    AutoDL租A100/4090 + 3000条精修数据
   │                    QLoRA微调 Qwen3.8-27B / Seed-OSS-36B
   │                    目的:得到"她"的第一版正式模型
   ▼
阶段 4(第7-8周, ~50元)  偏好优化 + 游戏Agent
   │                    DPO(500对好/坏回复) + mineflayer接入Minecraft
   │                    目的:人味二段打磨,游戏陪玩上线
   ▼
阶段 5(长期, ~30元/月)  数据飞轮
                        聊天记录回流 → 精修 → 季度迭代一版
                        目的:她越用越像"她"
```

每个阶段的具体操作分散在对应章节:阶段 0-1 见第 6/8 章,阶段 2-3 见第 3/4/7 章,阶段 4 见第 6.4 节,阶段 5 见第 9 章。

## 0.4 预算总账

| 项目 | 金额 | 说明 |
|---|---|---|
| 学习期(Kaggle 免费卡练手) | 0 元 | Unsloth 官方 notebook 直接在 Kaggle 跑 |
| 本地冒烟测试 | 0 元 + 电费 | 4070TiS 16GB 跑 14B QLoRA |
| 云端正式微调(第一版) | 20~35 元 | A100 80G × 4 小时左右 |
| API 蒸馏生成语料 | 10~30 元 | DeepSeek/GLM API 批量生成几千条 |
| DPO 偏好优化(第二轮) | 15~25 元 | A100 × 2-3 小时 |
| 一个月内全部实验 | **≤ 300 元** | 按量付费,用完即关 |
| 长期运行(本地部署) | **电费 ≈ 50~90 元/月** | 见第 7.5 节电费账本 |
| 可选硬件升级(24G 卡) | 一次性投入 | 见第 7.2 节选卡指南 |

## 0.5 十条核心结论(赶时间只看这里)

1. **不要从零训练,不要全量微调**。前者是几百万起步的大厂游戏,后者对 27B 模型需要 8 张 A100。你要做的是 **QLoRA 微调**:只训约 0.3%~0.5% 的参数,单轮 20~35 元。
2. **参数量纠结"30B 够不够"的答案是:够**。陪伴场景,数据质量 >> 参数量。答不上来的问题用 RAG + 工具调用 + "诚实样本"解决,不靠堆参数。
3. **底座首选 Qwen3.8-27B**(2026.8 开源,Apache 2.0,中文人味好,生态最全);次选 **Gemma 4 26B/31B**(文风天花板);想要"30 稍高一点"选 **Seed-OSS-36B**(字节,36B,Apache 2.0)。
4. **本地日常运行用 MoE 模型**(如 Qwen3-Omni-30B-A3B,激活仅 3B),16GB 显存也能流畅跑,还能听语音;云端训练用 dense 模型,质量更稳。
5. **Base 版、Instruct 版的区别**:Base 是"读过书没学过聊天"的原始模型,Instruct 是"学过聊天"的对话模型。**微调一律从 Instruct 版开始**,除非你要做深度风格重塑。
6. **数据是真正的壁垒**:3000 条精修过的、语气统一的人设对话,比 90B 裸模型强。配比:自建人设数据 60% + 现成 RP 数据 20% + 通用保鲜数据 20%。
7. **混入 20-30% 通用数据是防"越训越笨"的关键**,另外要主动加 50~100 条"不懂就说不懂"的诚实样本,把胡编乱造从源头训掉。
8. **腾讯云训练太贵**(V100 按量 10-30 元/时且架构过时),训练去 **AutoDL**(4090 约 1.9 元/时,A100 80G 约 6.68 元/时);腾讯云这类大厂留到以后部署正式服务再说。
9. **你的两台消费级机器组不了训练集群**(没有 NVLink,网络带宽是死穴),正确姿势:台式机=本地推理+小模型实验,笔记本=客户端,训练交给云端按量租。
10. **架构焦虑不用有**:Transformer 主干十年未变,就算底层架构换代,你的"资产"(人设数据、记忆库、评估集)全部无损迁移,换底座重训一轮只要一杯奶茶钱。

---

# 第 1 章 大模型基础:把名词一次讲透

## 1.1 大模型的一生(训练流水线)

一个开源模型到你手里之前,经历了四个阶段。理解这条流水线,所有名词就都有了坐标系:

```
┌─────────────────────────────────────────────────────────────────┐
│ ① 预训练 Pre-training                                           │
│    喂几万亿 token 的互联网文本,目标只有一个:预测下一个词        │
│    产物:Base 模型("读过整个互联网,但不会好好说话")             │
│    成本:几百万~上亿人民币,几万张卡跑几个月 —— 与你无关          │
├─────────────────────────────────────────────────────────────────┤
│ ② 监督微调 SFT (Supervised Fine-Tuning)                         │
│    喂几万~几百万条"指令→高质量回复"的问答对                      │
│    产物:Instruct/Chat 模型("学会了一问一答,像个助手")          │
│    成本:大厂用 API 合成数据 + 人工标注,几千卡时                  │
│    ★ 你要做的"微调"就是这一步的缩小版:几千条数据 + 1张卡几小时   │
├─────────────────────────────────────────────────────────────────┤
│ ③ 偏好对齐 Alignment (RLHF / DPO / GRPO...)                     │
│    拿"人类更喜欢哪个回复"的对比数据,把模型调得更符合人类偏好    │
│    产物:最终发布版("更礼貌、更安全、更少废话")                  │
│    ★ 你的第二阶段:用 DPO 把"人味"再打磨一层                     │
├─────────────────────────────────────────────────────────────────┤
│ ④ 部署与推理 Inference / Serving                                │
│    量化压缩 → 打包成 GGUF/safetensors → 用 vLLM/Ollama 跑起来   │
│    ★ 你 90% 的日常:这一步                                       │
└─────────────────────────────────────────────────────────────────┘
```

关键洞察:**你手里的开源模型已经完成了 ①②③ 全部三步**。你的"训练"是在成品基础上做 ② 的迷你重演(教她人格)+ ③ 的迷你重演(教她人味)。这就是为什么单轮只要几十块钱——重活人家都干完了。

### 继续预训练(CPT)为什么你用不上

在 SFT 之前用海量领域文本(比如全部小说、全部直播转录)再跑一遍预训练,叫继续预训练(CPT)。它适合"给模型灌一整个领域的知识"(医疗、法律),特点是数据量要大(至少 GB 级文本)、容易破坏原有能力、成本是微调的 100 倍。陪伴场景的知识问题用 RAG 解决,永远轮不到 CPT。

### 蒸馏(Distillation)为什么值得你关注

让大模型(GPT-5/GLM-5.3/DeepSeek V4)生成训练数据,拿去教小模型,这叫蒸馏。它是**穷人的神器**:你根本没有标注团队,但 API 花几十块钱就能"雇"最强的模型当你的数据标注员。第 4 章的语料流水线本质就是一次 DIY 蒸馏。

另外注意:市面上不少开源小模型本身就是官方蒸馏的产物(比如 DeepSeek 把 R1 的推理能力蒸馏进小尺寸模型)。这解释了一个现象——**小模型的能力上限这几年被不断抬高**,30B 级模型在很多任务上已经逼近两年前的 70B,这也是"30B 够用"结论的底气之一。

## 1.2 术语大全(按场景分组速查)

### 训练阶段类

| 术语 | 一句话解释 |
|---|---|
| 预训练 Pre-training | 从随机初始化开始,海量文本学会语言本身 |
| 继续预训练 CPT | 在已有模型上继续海量灌域内文本 |
| SFT 监督微调 | 用问答对教模型"该怎么回" |
| 指令微调 | SFT 的同义词,侧重"听指令" |
| RLHF | 基于人类反馈的强化学习,对齐的经典方法,复杂且贵 |
| DPO | 直接偏好优化,RLHF 的简化替代,不需要奖励模型,你现在的主流选择 |
| GRPO | DeepSeek 带火的强化学习算法,去掉价值网络省显存,推理能力训练主流 |
| KTO / ORPO / SimPO | 其他偏好优化变体,数据需求更少,备选 |
| RLAIF | 用 AI 反馈代替人类反馈做对齐 |
| 蒸馏 Distillation | 大模型生成数据教小模型 |
| 合成数据 Synthetic Data | AI 生成的训练数据,2026 年训练数据的主流来源 |
| 灾难性遗忘 Catastrophic Forgetting | 微调太窄导致旧能力退化,陪伴模型第一杀手,第 4 章专门治它 |
| 过拟合 Overfitting | 把训练数据背下来了,泛化没了,表现为复读、生硬 |
| 欠拟合 | 训练不够,人设没学进去,表现为"还是原来的味" |
| 数据飞轮 | 使用→回流→精修→再训练的闭环 |

### 微调方法类

| 术语 | 一句话解释 |
|---|---|
| 全量微调 Full FT | 所有参数都训,效果上限最高,成本爆炸,27B 级别需要 8×A100 |
| PEFT | 参数高效微调的总称,LoRA 是其代表 |
| LoRA | 冻结原模型,只训低秩"补丁矩阵",产物几十~几百 MB |
| QLoRA | LoRA + 4bit 量化底座,显存再砍 60%,2026 年默认方案 |
| NF4 | QLoRA 用的 4bit 数据类型(Normal Float),比普通 INT4 更适合权重分布 |
| DoRA | LoRA 改良版,把权重分解成幅度+方向分别训练,质量略好 |
| rsLoRA | LoRA 改良版,秩缩放修正,大 rank 时更稳 |
| rank / 秩 | 补丁矩阵的"容量",决定可训练参数占比的旋钮 |
| alpha | LoRA 缩放系数,惯例取 2×rank |
| target modules | 给哪些层加补丁(attention / MLP / all) |
| merge / 合并 | 把补丁融回原模型,得到完整权重 |
| Adapter | 早期 PEFT 方案,在层间插小网络,已被 LoRA 系取代 |
| Prefix / P-tuning | 早期 PEFT 方案,训"虚拟前缀",同上,了解即可 |
| 模型合并 Model Merging | 把多个微调模型按权重融合(SLERP/TIES/DARE),RP 社区最爱,零训练成本 |
| mergekit | 模型合并的事实标准工具 |

### 量化与文件类

| 术语 | 一句话解释 |
|---|---|
| FP32 | 32 位浮点,祖传精度,训练早期用,现已少 |
| FP16 / BF16 | 16 位浮点,模型原始发布精度,1B 参数≈2GB |
| FP8 | 8 位浮点,Hopper/Blackwell 新卡硬件支持 |
| INT8 | 8 位整数量化,体积减半,精度损失小 |
| INT4 / Q4 | 4 位量化,体积 1/4,本地部署主流 |
| NF4 | QLoRA 专用 4bit 格式 |
| GGUF | llama.cpp 系的模型文件格式,CPU/GPU 混合推理标配,Ollama 吃这个 |
| safetensors | HuggingFace 标准格式(原始权重,未量化) |
| GPTQ / AWQ | 两种 GPU 推理用量化算法,AWQ 精度通常更好 |
| EXL2 / EXL3 | ExLlamaV2/V3 的量化格式,显存利用率高 |
| Q4_K_M 等 | GGUF 的量化等级命名,1.4 节详解 |
| bitsandbytes (bnb) | 集成在 transformers 里的易用量化库,QLoRA 训练用它 |

### 推理与部署类

| 术语 | 一句话解释 |
|---|---|
| 推理 Inference | 拿模型跑对话/生成,与训练相对 |
| vLLM | 高性能推理服务器,并发强,云端部署主流 |
| llama.cpp | 纯 C++ 推理引擎,GGUF 之家,本地端设备的根基 |
| Ollama | llama.cpp 的傻瓜化封装,一条命令跑模型 |
| LM Studio | 图形界面版 Ollama,小白友好 |
| KV Cache | 缓存已处理文本的中间结果,加速长对话,吃显存大户 |
| 上下文长度 | 模型一次能"看见"的最大文本量,以 token 计 |
| RoPE | 位置编码方案,上下文长度的技术根基,不用深究 |
| MoE | 混合专家,总参数大但每次只激活一部分,推理便宜 |
| 激活参数 | MoE 每次真正参与计算的参数量,决定速度 |
| 投机解码 | 用小模型草稿+大模型验证加速生成 |
| 批处理 Batch | 一次喂多条请求,服务器端吞吐关键 |
| 并发 | 同时服务几个对话 |

### 应用层类

| 术语 | 一句话解释 |
|---|---|
| RAG | 检索增强生成:先查资料再回答,治"答不上来" |
| Embedding | 文本转向量,RAG 的地基 |
| 向量数据库 | 存 Embedding 的库:Qdrant/Milvus/Chroma/pgvector |
| 重排 Rerank | 检索后精排,提升 RAG 命中质量 |
| Agent | 让模型自主规划+调用工具完成任务 |
| Function Calling | 模型按格式输出"调用哪个工具"的结构化指令 |
| MCP | 模型上下文协议,2025 年起工具接入的事实标准 |
| 长期记忆 | 跨对话记住信息:Mem0 / Letta(MemGPT)/ Zep |
| 系统提示词 System Prompt | 对话开头的"宪法",设定身份规则 |
| 人设卡 | SillyTavern 生态的角色设定卡 |
| SillyTavern | 角色扮演前端事实标准 |
| VLM | 视觉语言模型,能看图/看屏幕 |
| TTS / ASR | 文本转语音 / 语音转文本 |

### 训练工程黑话(租卡时见到不慌)

| 术语 | 一句话解释 |
|---|---|
| 混合精度 AMP | 训练时一部分用 16bit 省显存提速 |
| 梯度累积 | 显存小就分几步攒梯度再更新,等效大 batch |
| 梯度检查点 | 用重算换显存,速度换 30% 显存 |
| DeepSpeed ZeRO 1/2/3 | 微软的显存分摊方案,多卡训大模型用 |
| FSDP | PyTorch 官方的 ZeRO 类似物 |
| Flash Attention 2 | 注意力算子优化,长文本必备,你的 4070 支持 |
| 学习率 lr | 每步参数改多大,QLoRA 常用 1e-4 ~ 2e-4 |
| warmup | 开头几步小学习率热身,防爆 |
| cosine 调度 | 学习率按余弦曲线衰减到 0 |
| epoch | 全部数据过几遍,陪伴 SFT 用 2~3 |
| cutoff / max_seq_len | 训练时截断的最大序列长度,2048 起步 |
| checkpoint | 训练中途存档 |
| 竞价实例 | 云上打折的"可能被抢占"机器,训练慎用,推理可用 |

## 1.3 模型文件版本详解(重点:Base / Instruct / Chat / -it / GGUF / Q4_K_M)

你在 HuggingFace / ModelScope 上会看到眼花缭乱的名字,比如:

```
Qwen3.8-27B              ← 什么都不带
Qwen3.8-27B-Base         ← 底座版
Qwen3.8-27B-Instruct     ← 对话版(指令微调过)
Qwen3.8-27B-Instruct-2508 ← 带日期的版本号(2508 = 2025年8月发布)
Qwen3.8-27B-Thinking     ← 推理增强版(会先想后答)
gemma-4-26b-it           ← Google 的对话版后缀是 -it(instruction-tuned)
Mistral-Small-3.2-24B-Instruct-2506
Qwen3.8-27B-Instruct-Q4_K_M.gguf  ← 量化版文件
Qwen3.8-27B-Instruct-AWQ  ← AWQ 量化版
```

拆解规则如下。

### 1.3.1 训练阶段后缀(最重要,选错全盘皆输)

| 后缀 | 全称 | 是什么 | 什么时候用 |
|---|---|---|---|
| (无后缀) / **Base** | 基础模型 | 只做完预训练的"原始大脑"。你问它话,它可能续写而不是回答,可能答一半停下来。它不是坏的,只是没学过"聊天"这个技能 | ❌ 微调新手别碰。只有做深度风格重塑(用海量小说文风改写模型气质)或研究时才用 |
| **Instruct** / **Chat** / **-it** | 指令微调版 | 做完 SFT 的"学过聊天"版本。同一家的叫法不同:阿里叫 Instruct,其他家有的叫 Chat(旧版 ChatGLM),Google 叫 -it | ✅ **99% 的情况用这个**,你的微调底座就是它 |
| **Thinking** / **Reasoning** / **R** | 推理版 | 回答前先生成一段思考过程(思维链)。数学/代码强,但:说话啰嗦、响应慢、把"想的过程"当成风格输出会毁人设 | ⚠️ 陪伴场景**不用**。例外:游戏决策模块可以单独用一个小推理模型 |
| **Distill** | 蒸馏版 | 大模型的能力蒸馏进小模型 | 可用,本质还是普通模型 |
| **Coder** | 代码特化 | 代码强,闲聊人味弱 | 陪伴场景不用 |
| **VL / Omni / Audio** | 多模态 | 能看图(VL)/ 能听能说能看(Omni) | ✅ 陪伴场景加分项,能语音对话、看游戏画面 |
| **Guard / Safety** | 安全审核 | 用来审核其他模型输出的分类器 | 你用不上 |
| **exp / Preview / Alpha** | 实验版 | 不稳定的前瞻版本 | 观望,不当主力 |

**一句话总结:你的微调底座 = 最新的 Instruct 版(或 -it 版)。Base 版是给进阶玩法的,Thinking 版是陪伴场景的反面教材。**

### 1.3.2 为什么 Base 版在新手手里会"翻车"

理解这一点,你对整个流水线的理解就到位了。Base 模型的本质是"互联网文本续写机"。你问"今天心情不好怎么办",它最可能的行为是:

- 续写出论坛里类似提问的后面几层楼
- 写一半就停(因为它见过的训练文本经常在句中断开)
- 你说"你好",它接"你好,请问有什么可以帮您?我是智能客服小助手……"(它见过大量客服语料)

而 Instruct 版被人类教过"收到问题→好好回答"这个行为模式。**你的 LoRA 微调是在 Instruct 版上叠加"她是谁"的人格层,而不是教一个原始大脑从零学会说话。**

### 1.3.3 文件格式后缀

| 格式 | 是什么 | 配套工具 | 你的使用场景 |
|---|---|---|---|
| `.safetensors` | HF 标准的原始权重格式(通常 BF16),安全(不可执行任意代码)、加载快 | transformers / vLLM / LLaMA-Factory / 训练全流程 | 云端训练、合并模型时打交道的就是它 |
| `.bin` | 旧版 PyTorch 权重 | 同上 | 新模型基本看不到了,忽略 |
| `.gguf` | llama.cpp 系格式,**量化与原权重的容器**,单文件包含全部 | Ollama / llama.cpp / LM Studio / Jan | 本地部署的主力格式 |
| `-AWQ` | AWQ 算法量化版(通常 INT4) | vLLM(支持最好)/ AutoGPTQ | 云端高并发部署 |
| `-GPTQ` | GPTQ 算法量化版 | 同上(较老) | 被 AWQ 取代中 |
| `-EXL2/-EXL3` | ExLlamaV2/V3 格式,可自定义位宽 | ExLlamaV2 / TabbyAPI | 显存极限压榨党 |
| `.mlx` | Apple 芯片专用格式 | MLX 框架 | Mac 用户的事,与你无关 |

**你的世界只需要两个:safetensors(训练时)+ GGUF(部署时)。** 云上训完 → merge → 转 GGUF → 拿回本地 Ollama 跑,这条链路是第 8 章的主线。

### 1.3.4 一条完整文件名的解剖

```
Qwen3.8-27B-Instruct-Q4_K_M.gguf
│         │    │        │
│         │    │        └─ 量化等级:4bit 块量化(K 量化中位精度版)
│         │    └─ 训练阶段:指令微调版(会聊天)
│         └─ 参数量:约 270 亿参数
└─ 家族+代际:Qwen 3.8 代
```

## 1.4 量化详解(INT8 / INT4 / Q4_K_M 到底差多少)

### 1.4.1 原理一句话

模型权重本来是 BF16 浮点数(每个数 2 字节)。量化的本质:**用更少的比特存每个数,同时尽量不让模型变笨**。就像把无损音乐压成 MP3——体积大减,好耳朵能听出区别,普通人无感。

### 1.4.2 精度阶梯与体积换算

通用公式:**显存/文件大小 ≈ 参数量(B) × 每参数字节数**。

| 精度 | 字节/参数 | 27B 模型大小 | 用途 |
|---|---|---|---|
| FP32 | 4 | ~108 GB | 上古时代,忽略 |
| BF16 / FP16 | 2 | ~54 GB | 原始发布精度,训练和云端全精度推理 |
| FP8 | 1 | ~27 GB | 新卡推理 |
| INT8 / Q8_0 | ~1 | ~28 GB | 近无损量化 |
| Q6_K | ~0.8 | ~22 GB | 接近无损 |
| **Q5_K_M** | ~0.68 | ~19 GB | 质量与体积的好平衡 |
| **Q4_K_M** | ~0.57 | **~16.5 GB** | **本地部署默认档**,你的 4070TiS 刚好卡着跑 |
| Q4_K_S / IQ4_XS | ~0.5 | ~14.5 GB | 显存紧张再降一档 |
| Q3_K_M | ~0.44 | ~12.5 GB | 开始明显变笨,陪伴场景不推荐 |
| Q2_K | ~0.33 | ~10 GB | 灾难级降智,忽略 |

经验法则:**Q4_K_M 是质量-体积的甜点;Q5_K_M 是"够得着就上"的更优解;低于 Q4 需要警惕;Q8/Q6 用于"显存富裕求心安"。**

### 1.4.3 GGUF 量化等级命名解码

`Q4_K_M` 拆开是:Q(量化)+ 4(bit 数)+ K(使用 K-quant 混合精度块)+ M(中位 Medium,还有 S=Small、L=Large 变体)。IQ 开头(iQ4_XS 等)是更新的重要性矩阵量化,同体积质量略好。不用背,记住 **Q4_K_M / Q5_K_M / Q6_K / Q8_0 这四档**就覆盖全部日常。

### 1.4.4 量化对"人味"的影响

陪伴场景对量化比工具场景敏感,因为语气细微差别是模型的"高分辨率"能力:

- Q8/Q6:无感差异
- Q4_K_M:90% 的对话无感,偶尔细微的情绪层次变平
- Q3 及以下:明显变笨 + 语气发干,毁人设重灾区

结论:**你的主模型最低 Q4_K_M,能上 Q5 更好;鼓捣量化压缩省的那几个 GB,不如换来语气保真。**

### 1.4.5 量化 vs 微调的关系(新手易混)

- 量化是**部署/训练时的压缩技术**,不改变模型"会什么"
- 微调是**改变模型行为**的技术
- QLoRA = 量化(压缩底座省显存)+ 微调(LoRA 补丁)的组合拳:底座压成 4bit 冻住,补丁用 BF16 训练

## 1.5 参数量、Dense 与 MoE(专家结构)

### 1.5.1 参数量意味着什么

参数量 ≈ 模型的"神经连接数量",粗略对应"知识存储容量 + 能力上限"。但注意三个反直觉事实:

1. 参数量不等于质量:训练数据质量、数据配比、后训练功夫的权重逐年上升,近年 30B 模型打平多年前 70B 是常态
2. 参数量不直接等于"懂多少事":长尾知识靠检索(RAG)补,不靠参数硬记
3. 参数量直接决定:显存需求、生成速度、训练成本——**它主要是个"钱"的指标**

### 1.5.2 Dense(稠密)vs MoE(混合专家)

```
Dense(稠密模型):              MoE(混合专家模型):
每个 token 都经过全部参数        每个 token 只路由到少数"专家"

输入 → [全部 27B 参数] → 输出     输入 ┬→ [专家1] ┐
                                      ├→ [专家3] ┤→ 合并 → 输出
速度:慢,与总参数成正比          └→ (其他专家睡觉)
质量:每 token 计算量大,稳       速度:快!只与激活参数成正比
显存:全参数都要加载              显存:全参数都要加载(专家占地方)
                                      但计算量只看激活部分
```

用个比喻:Dense 是一个每道题都亲自算的老教授;MoE 是一个专家会诊团队,每个问题只叫醒相关的几位专家,但所有专家的工资(显存)都得照付。

关键数字对比(同样的显存预算):

| | Dense 27B | MoE 30B-A3B |
|---|---|---|
| 总参数 | 27B | 30B |
| 激活参数 | 27B | **仅 3B** |
| 生成速度(Q4,消费卡) | ~25-35 token/s | **~80-120 token/s** |
| 知识广度 | 中 | 较高(总参数大) |
| 每字深度 | 较深 | 较浅(计算少) |
| 微调难度 | 低,工具最成熟 | 中,MoE 微调工具链在成熟中 |
| 适合 | 云端正式版、追求质量 | **本地日常运行、实时语音、直播弹幕实时回复** |

### 1.5.3 你的双轨策略(结论)

- **本地 16GB 日常跑:MoE**(激活小速度快,实时语音/直播不会卡成机器人)
- **云端训练主力:dense 26-36B**(工具链最顺,质量上限高,训出来当"正式版大脑")
- 两者的**人格数据完全通用**(同格式 JSON),换底座=重新租一次卡,第 9 章详述

## 1.6 上下文长度与 Token

### 1.6.1 Token 是什么

模型处理文本的基本单位,一个汉字通常是 1~2 个 token,一个英文单词 1~1.5 个。粗算:**中文 1 字 ≈ 1.5 token**(各家 tokenizer 有差异)。

1 万字中文 ≈ 15000 token。27B 模型的上下文一般 32K~256K(≈ 2万~17万字中文),陪伴场景绰绰有余——但注意上下文越长,推理越慢、KV cache 越吃显存,实际对话不是越长越好。

### 1.6.2 上下文的"海绵效应"(重要认知)

模型不会永远记住所有历史。单次会话内,太老的对话要么被截断、要么被你的记忆系统摘要压缩。**"她记得你"这件事=记忆系统的工程,不是模型自带的**,第 6 章专门讲。

### 1.6.3 Token 怎么算钱(用 API 蒸馏语料时)

按"输入 token + 输出 token"计费。生成 3000 条对话语料,每条平均 800 token 输出 ≈ 240 万输出 token,用 DeepSeek/GLM 的便宜档大概 10~30 元人民币。这就是"API 蒸馏"的成本量级——一杯奶茶雇了个最强标注员。

## 1.7 采样参数:决定"她说话的感觉"的旋钮

这些不是训练参数,是**推理时**的参数,直接影响人味,值得单独一节:

| 参数 | 作用 | 陪伴场景建议 |
|---|---|---|
| temperature | 随机性。低=稳定死板,高=放飞乱说 | **0.7~0.9**(人味的关键旋钮之一) |
| top_p | 候选词概率截断 | 0.9~0.95 |
| top_k | 候选词数量截断 | 40~50(或不管,只用 top_p) |
| repeat_penalty | 重复惩罚 | 1.05~1.1,**别太高**(超过 1.2 会把口头禅惩罚没了,毁人设) |
| min_p | 动态概率门槛(更新的方案) | 0.05~0.1,可替代 top_p |
| max_tokens | 单条回复长度上限 | 300~800(陪伴对话要短,回复太长像写作文) |

**陪伴场景的隐性共识:回复要短**。真人聊天没人发 500 字小作文。SillyTavern 社区普遍用"每段 1-3 句话"的指令约束 + 采样参数控制,你的训练数据也应该按"短回复"来写(第 4 章数据规范的依据之一)。

# 第 2 章 模型选型:2025.6 ~ 2026.9 开源模型全景清单

> 本章窗口:2025 年 6 月 ~ 2026 年 9 月。原则:**有商用限制的直接 pass,不稀罕**;MoE 专家结构一并纳入;下载渠道含魔搭(ModelScope)。
> ⚠️ 许可证与具体参数请以各家模型卡(HuggingFace/ModelScope 页面)为准,下表是选型地图,不是法律意见书。

## 2.1 选型四原则

1. **许可先行**:Apache 2.0 / MIT = 绿灯随便用;Gemma 类宽松自定许可 = 黄灯(允许商用但有附加条款,个人+直播无碍);任何 NC(Non-Commercial)非商用许可 = 红灯直接 pass
2. **Instruct 版优先**:底座选最新一代的对话版(1.3 节),不选 Base
3. **生态权重 > 跑分**:能被 LLaMA-Factory/Unsloth 一键加载、社区有现成微调和量化 GGUF 的模型,优先于跑分高但没人伺候的模型
4. ** dense 和 MoE 都要**:dense 当训练主力,MoE 当本地运行主力(1.5 节双轨策略)

## 2.2 许可证红绿灯总表

| 许可证 | 灯 | 哪些模型 | 你的场景能用吗 |
|---|---|---|---|
| Apache 2.0 | 🟢 | Qwen 全系、Seed-OSS、Granite 4.0、gpt-oss、Mistral 全系 | 随便用,可商用可直播 |
| MIT / 改造版 MIT | 🟢 | DeepSeek 系、Kimi Linear | 同上 |
| Gemma License | 🟡 | Gemma 全系 | 允许商用,附使用政策;个人使用/直播无碍 |
| Llama 4 Community | 🟡 | Llama 4 全系 | 月活<7 亿免谈;个人无碍,但不算真"开源" |
| EXAONE License | 🔴 | EXAONE 4.0 | **非商用,pass** |
| CC-BY-NC / OpenRAIL-NC 类 | 🔴 | 部分社区小模型 | 非商用,pass |
| GPL/AGPL 类 | 🟡 | 少量模型 | 权重本身基本无此问题,注意配套代码即可 |

## 2.3 主力档:26B ~ 40B Dense(训练主力候选池)

这是你的核心目标区间,按发布时间排序:

| 模型 | 发布 | 参数 | 许可 | 上下文 | 亮点 | 短板 |
|---|---|---|---|---|---|---|
| **Qwen3-32B** | 2025.4(窗口略早,基石收录) | 32B | Apache 2.0 | 128K | 上代 30B 档标杆,微调教程最多,LLaMA-Factory 全流程文档齐全 | 不是最新一代 |
| **Mistral Small 3.2 24B** | 2025.6 | 24B | Apache 2.0 | 128K | 欧洲系标杆,英文文风好,推理快 | 中文略逊于 Qwen |
| **gpt-oss-20b** | 2025.8 | 21B MoE(激活 3.6B) | Apache 2.0 | 128K | OpenAI 开源亲儿子,原生 MXFP4 量化,16G 卡全速跑 | 推理系风格重,人味要花功夫调教 |
| **Seed-OSS-36B** | 2025.8(字节) | 36B | Apache 2.0 | 512K | "30 稍高一点"的最佳答案,长上下文天花板,中文好 | 社区微调生态一般,RP 玩家少 |
| **Qwen3-32B( Thinking 版)** | 2025 | 32B | Apache 2.0 | 128K | 同模型推理增强版 | 陪伴场景用不上,列出只为排雷 |
| **Qwen3-VL-32B** | 2025.10 | 32B 视觉 | Apache 2.0 | 128K | 能看图/看屏幕 | 陪伴主力之外的视觉补位 |
| **Gemma 4 26B** ⭐ | 2026.4 | 26B | Gemma License | 128K+ | 2026 年 30B 级公认最强之一,LMArena 高分,RP 社区微调爆发中 | 需留意 Gemma 附加条款 |
| **Gemma 4 31B** ⭐ | 2026.4 | 31B | Gemma License | 128K+ | 同上,更大更聪明,Q4 约 19GB | 16G 本地推理需部分 offload |
| **Qwen3.8-27B** ⭐⭐ | 2026.8 | 27B | Apache 2.0 | 128K+ | 最新一代 Qwen 中杯,中文人味/指令遵从全面升级,发布即有社区微调 | 太新,踩坑资料还在积累 |

**小结**:中文陪伴首选 **Qwen3.8-27B**;英文文风/国际社区玩法选 **Gemma 4 26B**;想要"30 稍高"选 **Seed-OSS-36B**;求稳抄作业选 **Qwen3-32B**。

## 2.4 专家结构档:MoE(本地运行主力候选池)

| 模型 | 发布 | 总参/激活 | 许可 | 亮点 | 短板 |
|---|---|---|---|---|---|
| **Qwen3-30B-A3B** | 2025.4 | 30B/3B | Apache 2.0 | 本地 16G 流畅跑的甜点,Instruct/Thinking 双版本,生态极成熟 | 上代架构 |
| **Qwen3-Omni-30B-A3B** ⭐ | 2025 | 30B/3B | Apache 2.0 | **端到端多模态:文本+图像+音频进,文本+语音出**,陪伴场景神器(能听你说、看屏幕、直接出语音) | 部署比纯文本复杂,微调工具支持在跟进 |
| **gpt-oss-120b** | 2025.8 | 117B/5.1B | Apache 2.0 | OpenAI 旗舰开源,知识量大 | 超出你的部署预算,单卡 80G 才舒服 |
| **IBM Granite 4.0 (32B-A9B)** | 2025.10 | 32B/9B | Apache 2.0 | Mamba 混合架构,长文本显存省 | 社区 RP 生态薄弱 |
| **Kimi Linear 48B-A3B** | 2025.10 | 48B/3B | 改造版 MIT | 线性注意力混合,超长上下文省显存 | 新架构微调工具支持不成熟,观望 |
| **Qwen3-Next 80B-A3B** | 2025.9 | 80B/3B | Apache 2.0 | 总参 80B 知识面广,激活 3B 推理快 | 训练显存门槛高(A100 80G 级),先收藏 |
| **Qwen3.8-Flash-Next** | 2026.8 | MoE | 开放权重 | Qwen4 架构前瞻,多模态 | 刚发布,追新观望 |
| **Nemotron 3 Nano 30B** | 2026 | 30B | NVIDIA 开放 | 自托管榜单常客 | 社区微调少 |

**小结**:本地日常 + 语音陪伴选 **Qwen3-Omni-30B-A3B**;纯文本选 **Qwen3-30B-A3B**;这两颗和 dense 主力共用你的一套人格数据。

## 2.5 轻量档:8B ~ 14B(冒烟测试/移动端)

| 模型 | 发布 | 参数 | 许可 | 用途 |
|---|---|---|---|---|
| Qwen3-14B | 2025.4 | 14B | Apache 2.0 | **本地冒烟测试首选**(4070TiS QLoRA 完全装得下),人格数据与 27B 通用 |
| Qwen3-8B | 2025.4 | 8B | Apache 2.0 | 笔记本 4070 8GB 能跑的极限,移动客户端备选 |
| Gemma 4 12B 级 | 2026 | ~12B | Gemma License | 同窗口新轻量,备选 |
| Qwen3.8-Flash 级小杯 | 2026 | ~10B | Apache 2.0 | 最新小杯,追新备选 |

## 2.6 旗舰档(100B+,你的"API 蒸馏供应商"和"混合大脑兜底")

不部署,只通过 API 用(蒸馏语料 + 疑难问题兜底),2026.9 时点的旗舰:

| 模型 | 许可 | 备注 |
|---|---|---|
| GLM-5.3 | 开放权重 | 2026.9 开源榜第一梯队 |
| DeepSeek V4 | MIT | 性价比标杆,API 便宜,蒸馏语料主力候选 |
| Kimi K3 | 开放权重 | SWE-Bench/GPQA 类榜单顶尖 |
| Qwen3.8-Max | 2.4T 总参/95B 激活 | 2026.8 发布,API 可用 |
| GPT-5 / Claude 系 | 闭源 API | 蒸馏数据质量好,看预算 |

**用法**:生成训练语料、当"AI 评委"给回复打分、当疑难问题的兜底大脑。单次调用几分钱,一个月 10~30 元封顶。

## 2.7 排除名单(有商用限制或不适配,不稀罕)

| 模型 | 排除原因 |
|---|---|
| EXAONE 4.0 32B | 非商用许可 |
| 各类 CC-BY-NC 数据衍生的微调模型 | 传染性非商用 |
| Mistral Small 4 (119B) | 尺寸跳档,部署成本不符 |
| DeepSeek R1 系 Thinking 版做陪伴底座 | 思维链风格毁人设(当蒸馏老师没问题) |
| 一切 Base 版 | 新手禁区(1.3.2 节) |

## 2.8 最终推荐(决策树)

```
你的需求是中文陪伴 + 游戏陪玩 + 直播
│
├─ 云端训练的"正式大脑" → Qwen3.8-27B (Apache 2.0)
│    └─ 备选:Seed-OSS-36B(想要36B)/ Gemma 4 26B(玩英文社区)
│
├─ 本地日常运行的"值班大脑" → Qwen3-Omni-30B-A3B(要语音)
│    └─ 备选:Qwen3-30B-A3B(纯文本)/ gpt-oss-20b(显存紧张)
│
├─ 冒烟测试 → Qwen3-14B(本地 4070TiS 直接训)
│
└─ 蒸馏老师 + 兜底 API → DeepSeek V4 / GLM-5.3
```

## 2.9 去哪下载

| 渠道 | 地址 | 说明 |
|---|---|---|
| HuggingFace | huggingface.co | 全球主站,原始权重+社区量化 |
| **ModelScope 魔搭** | modelscope.cn | **国内直连不翻墙**,主流模型全有,`modelscope download` 命令行工具 |
| hf-mirror | hf-mirror.com | HF 的国内镜像,设个环境变量即可加速 |
| unsloth/bartowski 的量化仓库 | HF 内搜 `unsloth/Qwen3.8-27B-GGUF` 等 | 现成 GGUF 量化版,不用自己压 |

国内下载姿势(二选一):

```bash
# 方式一:ModelScope CLI(推荐,国内速度最好)
pip install modelscope
modelscope download --model Qwen/Qwen3.8-27B-Instruct --local_dir ./Qwen3.8-27B

# 方式二:huggingface-cli + 国内镜像
pip install -U huggingface_hub
export HF_ENDPOINT=https://hf-mirror.com
huggingface-cli download Qwen/Qwen3.8-27B-Instruct --local-dir ./Qwen3.8-27B
```

# 第 3 章 微调技术详解:LoRA / QLoRA / DPO

## 3.1 为什么是 QLoRA:一张表终结选择困难

以 27B 模型、训练 4 小时为例(2026.9 云端价格):

| 方案 | 需要的卡 | 卡时成本 | 效果 | 结论 |
|---|---|---|---|---|
| 全量微调 | 8×A100 80G | ~4,000+ 元 | 上限最高 | ❌ 贵到离谱,还容易遗忘 |
| LoRA(BF16 底座) | 4×A100 80G | ~2,000 元 | 接近全量 | ❌ 还是贵 |
| **QLoRA** | **1×A100 80G 或 1×4090(小模型)** | **20~35 元** | **约 95% 全量效果** | ✅ **你的答案** |
| QLoRA(本地) | 4070TiS 16GB(14B 级) | 电费 | 同上 | ✅ 冒烟测试 |

**QLoRA 的效果为什么能接近全量**:微调"教会模型新行为"主要靠低秩的方向性调整,LoRA 的补丁矩阵已经够用;底座压成 4bit 损失的是"底座固有精度",而底座是冻结不动的,补丁在训练中会自适应补偿。2026 年这是业界默认,Qwen/Gemma 官方社区微调示例全用它。

## 3.2 LoRA 原理(不需要数学的版本)

模型微调,本质是给几亿个权重各自找一个"改动量 ΔW"。全量微调就是把 ΔW 一个个学出来——参数太多所以贵。

LoRA 的洞察:实际微调时,ΔW 矩阵是"低秩"的——变化有规律,可以被拆成两个瘦矩阵的乘积:

```
ΔW (27000×27000 的巨大矩阵)
        ≈  A (27000×32) × B (32×27000)     ← rank=32 时

全量微调要学:27000 × 27000 ≈ 7.3 亿个新参数
LoRA 要学:  27000 × 32 × 2  ≈ 173 万个新参数(约 0.24%)
```

训练结束后,B×A 的结果加回原权重(merge),模型行为改变,但"她是谁"的核心知识原封未动——这同时就是**抗遗忘**的机理:原模型权重一个都没动,只是叠了个方向性微调。

## 3.3 QLoRA 拆解(三个组件)

1. **4bit NF4 底座**:把冻结的原模型压成 NF4(Normal Float 4bit,按权重分布设计的 4bit 格式,比普通 INT4 更保真)
2. **双重量化**:连量化参数本身也量化一次,再省一点
3. **分页优化器**:显存峰值过高时把优化器状态临时挪到内存,防 OOM(爆显存)救命符

三个开关在 LLaMA-Factory 里对应:`quantization_bit: 4`、`double_quantization: true`、`paged_adamw` 系列参数。Unsloth 默认全给你配好。

## 3.4 DoRA 与 rsLoRA(顺手开上)

- **DoRA**:把权重分解成"幅度+方向"两个部分,方向用 LoRA 训、幅度单独微调。质量略好于原版 LoRA,速度略慢,2026 年的新教程基本默认带。`use_dora: true` 一行开关。
- **rsLoRA**:修正大 rank 时的缩放问题,`lora_alpha` 用 `rank×√2` 而不是 `2×rank`。rank≤64 时差异不大,了解即可。

陪伴场景建议:**开 DoRA,rank 32~64,其他不动。**

## 3.5 训练参数全解(每个都告诉你该填什么)

这是可以直接抄的 LLaMA-Factory 配置(陪伴人设微调 27B 级,Q4090/A100 皆可):

```yaml
### 模型
model_name_or_path: Qwen/Qwen3.8-27B-Instruct   # 底座,Instruct 版!
quantization_bit: 4                              # QLoRA 的 4
quantization_method: bitsandbytes                # NF4 走 bnb

### LoRA
finetuning_type: lora
lora_rank: 32                # 容量:16保守/32标准/64风格重/128大改
lora_alpha: 64               # = 2 × rank
lora_dropout: 0.05
lora_target: all             # attention+MLP 全加(陪伴场景推荐)
use_dora: true

### 数据
dataset: my_companion_v1     # 在 dataset_info.json 注册的名字
template: qwen               # ⚠️ 必须和底座模型匹配!(见下)
cutoff_len: 2048             # 单条样本最大长度;直播弹幕场景 1024 够
preprocessing_num_workers: 16

### 训练
per_device_train_batch_size: 2      # 单卡一次喂几条
gradient_accumulation_steps: 8      # 攒 8 步更新一次 → 有效 batch=16
learning_rate: 1.0e-4               # QLoRA 黄金档 1e-4~2e-4
num_train_epochs: 3.0               # 2~3 轮,多了过拟合
lr_scheduler_type: cosine
warmup_ratio: 0.05                  # 前 5% 步热身
bf16: true
gradient_checkpointing: true        # 省显存,速度慢 30%,24G 卡必开
logging_steps: 10
save_steps: 500                     # 中途存档
output_dir: saves/companion-v1
plot_loss: true
```

**各参数"什么时候调"速查**:

| 参数 | 默认建议 | 什么情况调 |
|---|---|---|
| lora_rank | 32 | 人设学不进去 → 64;数据少(<1500 条) → 16 |
| lora_alpha | 2×rank | 不懂别动 |
| lora_target | all | 显存极限时退到 `q_proj,k_proj,v_proj,o_proj` |
| learning_rate | 1e-4 | 过拟合(复读) → 5e-5;人设太淡 → 2e-4 |
| num_train_epochs | 3 | 数据 >5000 条 → 2;数据 <1500 条 → 4 |
| cutoff_len | 2048 | 有超长剧情样本 → 4096(显存↑) |
| 有效 batch size | 16 | 显存炸 → 减 batch、加 accumulation,乘积不变 |
| 温度相关采样 | — | 训练时不管,推理时调(1.7 节) |

**⚠️ template 必须匹配底座**:Qwen 系用 `qwen`,Gemma 用 `gemma`,不同模板 = 训练时说话格式和推理时不一致 = 模型精神分裂。LLaMA-Factory 的 README 有完整对照表。

## 3.6 "训练百分之多少"专节:占比是怎么算出来的

你问过:"微调应该有百分比的吧,对模型哪百分之多少进行取舍?"——有,但不是直接选百分比,而是由 **rank × target modules** 决定,公式:

```
可训练参数 ≈ Σ(每个被选中的线性层) [ d_out × rank + rank × d_in ]
占比 ≈ 可训练参数 / 总参数
```

以 27B 模型、rank=32、target=all(QKVO + 3 个 MLP 投影)为例:

| 配置 | 可训练参数量 | 占比 | 产物 adapter 大小 |
|---|---|---|---|
| rank=16, only attention | ~0.3 亿 | ~0.1% | ~60 MB |
| **rank=32, all** | **~1.2 亿** | **~0.45%** | **~250 MB** |
| rank=64, all | ~2.4 亿 | ~0.9% | ~500 MB |
| rank=128, all | ~4.8 亿 | ~1.8% | ~1 GB |
| 全量微调 | 270 亿 | 100% | ~54 GB |

训练启动后日志里会打印一行 `trainable params: xxx || all params: xxx || trainable%: x.xxxx`——这就是你要看的那个百分比,亲眼确认。

**取舍哲学**:
- 占比小(0.1-0.5%):原模型能力保留最好,学"语气人设"足够 → **推荐区间**
- 占比大(1%+):能学更多"知识性内容",但遗忘风险↑、数据需求↑,陪伴场景没必要
- **人味不够,先怪数据,再怪 rank**(第 4 章)

## 3.7 训练工具对比

| 工具 | 一句话 | 适合你吗 |
|---|---|---|
| **LLaMA-Factory** | 中文社区第一,有 WebUI(LLaMA Board),点鼠标训练,支持全主流模型 | ✅ **主力,新手友好** |
| **Unsloth** | 提速 2~4 倍、省显存 30-70%,notebook 式,官方教程跟着抄就行 | ✅ **本地冒烟 + 学习首选**(Kaggle 免费 GPU 能跑) |
| axolotl | YAML 配置流,英文社区主流之一 | 备选 |
| trl (HuggingFace) | 库,代码流,灵活 | 写代码党备选 |
| MLX | Apple 芯片专用 | 与你无关 |
| mergekit | 模型合并(不是训练) | 进阶玩具 |

**推荐组合**:Unsloth(Kaggle 学 + 本地冒烟)→ LLaMA-Factory(云端正式训练,WebUI 盯参数)→ mergekit(想玩合并再说)。

## 3.8 完整操作流程(从数据到成品模型)

以云端 AutoDL 为例,本地同理:

```bash
# 0. 开机(A100 80G 或 4090 24G),选 PyTorch 2.x + CUDA 12.x 镜像

# 1. 拉代码 + 装环境(装完记得做成 AutoDL 镜像,下次秒开)
git clone https://github.com/hiyouga/LLaMA-Factory.git
cd LLaMA-Factory && pip install -e ".[torch,metrics]"

# 2. 数据注册:把你的 JSON 放进 data/,在 data/dataset_info.json 加一条
#    "my_companion_v1": { "file_name": "my_companion_v1.json" }

# 3. 冒烟(用 100 条数据跑 50 步,验证整条链路不报错,几分钟)
llamafactory-cli train examples/train_lora/companion_smoke.yaml

# 4. 正式训练(WebUI 更直观:llamafactory-cli webui)
llamafactory-cli train examples/train_lora/companion_v1.yaml

# 5. 合并(补丁融回底座,得到完整模型)
llamafactory-cli export examples/merge_lora/companion_v1_merge.yaml

# 6. 转 GGUF(带回家)
#    用 llama.cpp 的 convert_hf_to_gguf.py 转换,再用 quantize 压成 Q4_K_M/Q5_K_M
python convert_hf_to_gguf.py ./saves/companion-v1-merged --outfile companion-v1-f16.gguf
./llama-quantize companion-v1-f16.gguf companion-v1-q5_k_m.gguf Q5_K_M

# 7. 本地验证
ollama create luomi -f ./Modelfile    # Modelfile 里 FROM 指向你的 GGUF
ollama run luomi
```

**训练时长量级参考**(3000 条 × 3 epoch,cutoff 2048):14B + 4070TiS 本地 ≈ 2~4 小时;27B + A100 80G ≈ 3~5 小时;27B + 4090 24G ≈ 6~10 小时(显存紧,需梯度检查点全开)。

## 3.9 显存速查表(训练与推理)

### 训练显存(QLoRA,梯度检查点开)

| 模型 | 显存需求 | 你的哪张卡能跑 |
|---|---|---|
| 8B | ~8 GB | 笔记本 4070 8G(极限)、台式机随便 |
| 14B | ~12 GB | **台式机 4070TiS ✅(冒烟主力)** |
| 26B/27B | ~20-23 GB | 云 4090 24G(紧)/ A100 40G 稳 |
| 32B | ~22-26 GB | A100 40G/80G |
| 36B(Seed-OSS) | ~24-28 GB | A100 80G 推荐 |
| 30B-A3B(MoE) | ~18-20 GB | 云 4090 可跑 |
| 80B-A3B(MoE) | ~40+ GB | A100 80G,先不碰 |

### 推理显存(GGUF Q4_K_M,全装显存)

| 模型 | Q4_K_M 大小 | 16GB(4070TiS) | 24GB(未来 4090) |
|---|---|---|---|
| 8B | ~5 GB | 全速 | 全速 |
| 14B | ~9 GB | 全速 | 全速 |
| 26B | ~16 GB | 刚好/轻微 offload | 全速 |
| 27B | ~16.5 GB | 刚好/轻微 offload | 全速 |
| 30B-A3B MoE | ~18 GB | offload 一部分,速度仍快(激活小) | 全速 |
| 31-32B | ~19-20 GB | offload 多,偏慢 | 全速 |
| 36B | ~22 GB | ❌ | offload 少量可用 |
| 80B-A3B MoE | ~45 GB | ❌ | ❌(需 2×24G) |

> "offload" = 部分层放内存,llama.cpp 支持,速度打折但可用;MoE 模型 offload 后依然飞快,因为它激活的参数少。

## 3.10 第二阶段:DPO 偏好优化(人味精修)

第一轮 SFT 后她会说话了,但总有些回复"对但尬"。DPO 用"好回复 vs 坏回复"的对比数据,把口味调过来。

### 数据格式(三元组)

```json
{
  "conversations": [{"from": "human", "value": "我今天面试挂了…"}],
  "chosen": {"from": "gpt", "value": "啊…先抱一个。哪家?说说,我看看是题太难还是他们有眼无珠。"},
  "rejected": {"from": "gpt", "value": "面试失败是常见现象,建议您复盘面试过程,总结经验教训,提升自身能力,为下一次机会做好准备。加油!"}
}
```

(rejected 不必是烂回复,"像客服、像老师、太端着"的都算——DPO 学的是"她的口味"。)

### 配置要点

```yaml
stage: dpo                    # LLaMA-Factory 里从 sft 换成 dpo
finetuning_type: lora         # DPO 也走 LoRA,同样便宜
pref_beta: 0.1                # 温度参数,默认即可
learning_rate: 5.0e-5         # 比 SFT 小一半
num_train_epochs: 1.0         # 一轮就够
```

数据量:500~1000 对起步。来源:第一版模型试聊时,把"回得好/回得尬"的现场标记下来(第 9 章数据飞轮的原料)。

## 3.11 进阶玩具:模型合并(mergekit)

把两个微调好的模型按权重"杂交",可能得到兼得两家之长的孩子,零训练成本。RP 社区的 MythoMax 等名模就是这么来的。

```yaml
# mergekit 配置示例:你的模型 + 社区模型 5:5 线性混合
models:
  - model: ./saves/companion-v1-merged
  - model: some-community/rp-model
merge_method: linear
parameters:
  weight: 0.5
```

**定位**:好玩,可出意外惊喜,但不可控——当支线玩法,主线还是 SFT+DPO。

## 3.12 失败模式排查手册(症状 → 病因 → 处方)

| 症状 | 病因 | 处方 |
|---|---|---|
| 复读机,同一句话翻来覆去 | 过拟合;数据里有重复;epoch 太多 | 降 epoch 到 2;查重数据;lr 降到 5e-5;推理时 repeat_penalty 1.05-1.1 |
| "作为AI我无法…"又回来了 | 训练数据太少,AI 味没压下去 | 加数据,尤其多写"她该怎么说"的样本;系统提示词强化人设兜底 |
| 人设会了,但变笨了(常识答错) | 灾难性遗忘 | **混 20-30% 通用数据**(最重要的一条处方);降 rank |
| 语气对但很生硬,像背台词 | 数据多样性不足,句式太单一 | 补不同场景样本;推理时 temperature 提到 0.8-0.9 |
| 什么都不肯说,过度拒绝 | 底座安全对齐 + 数据里"边界样本"太多太狠 | 边界样本只写真正要拒的;选对齐较轻的底座(Gemma/Qwen 通常好调) |
| 中英夹杂/突然说英文 | 英文数据混太多;template 错 | 英文数据压到 20% 以内;检查 template 配置 |
| loss 降不下去 | 数据格式错;template 错 | 用 LLaMA-Factory 的 `llamafactory-cli train --do_train` 前先跑数据预览;肉眼检查 JSON |
| loss 一直很低但效果差 | 数据太简单/重复,模型背下来了 | 提高数据多样性,而非加 epoch |
| 显存 OOM | — | 减 batch、开梯度检查点、降 cutoff、用 paged optimizer、换更小模型 |
| 训练到一半速度骤降 | paged 优化器在内存和显存间倒腾;过热降频 | 减 batch;云服务器一般无此问题,本地注意散热 |

## 3.13 训练完了怎么知道好不好

**不要看 loss**。loss 只能告诉你"学没学进数据",不能告诉你"像不像她"。真正的评估三层:

1. **盲测集**(核心):留 50 条没参与训练的对话,训完逐条对比新旧模型。问题要覆盖:日常闲聊/情绪场景/知识问答/边界测试/游戏话题。评分维度:人味、记忆一致性、实用性、胡说率。
2. **AI 评委**:把同一批问题分别发给新旧模型,拿 GLM-5.3/DeepSeek V4 当评委,按人设一致性打分。便宜、快速、可复现,但只能当辅助。
3. **实弹试聊**:你自己聊半小时。你的体感就是最终标准——这是陪伴 AI 和其他 AI 的本质区别:**你是唯一的用户,你的满意就是指标**。

# 第 4 章 数据工程:数据集 / 聊天记录导出 / 蒸馏 / 精修

> 全手册最重要的一章。重复一遍核心定律:**人味 90% 来自数据质量,10% 才是训练技巧。** 模型和算力都是钱能买到的,数据是只有你能造的。

## 4.1 三层数据架构

| 层 | 内容 | 数量 | 来源 | 作用 |
|---|---|---|---|---|
| L1 人设对话(核心) | "她"的多轮对话 | 2000~6000 条 | 自建:蒸馏生成+精修 | 教会"她是谁、怎么说" |
| L2 风格补剂 | 现成 RP/陪伴数据集 | 500~2000 条 | 开源数据集(4.3 节) | 补场景多样性,稀释 AI 味 |
| L3 通用保鲜数据 | 普通指令问答 | 500~1500 条 | 公开通用数据集 | **防灾难性遗忘** |
| (后期) L4 偏好对 | 好/坏回复对比 | 500~1000 对 | 试聊回流 | DPO 精修口味 |

配比参考:L1 : L2 : L3 ≈ 60% : 20% : 20%。**L3 不能省**——没有它,训完她人设满分但连"明天周几"都会答错(第 3.12 节遗忘症的处方)。

## 4.2 "她"的数据里该有哪些场景(覆盖清单)

照着这个清单造数据,每类都要有,缺一类她就在那一类上"露馅":

**日常陪伴类**
- 早安/晚安的仪式感对话(固定一两句专属口头禅)
- 吃饭/睡觉/天气的日常关怀(短,别查户口)
- 分享日常:"今天看到一只很凶的猫"
- 情绪安抚:被批评了/emo了/失眠了(先共情→陪伴→不灌鸡汤)
- 互怼玩梗:被损了怎么反击(留 10~20% 调皮样本,温柔没有反差就是客服)

**陪伴学习类**
- 监督式陪伴:"该背单词了,今天 30 个,我在旁边看着你"
- 答疑但不代劳:讲思路,不直接甩答案
- 番茄钟/打卡互动话术
- **诚实样本:遇到难题说"这个我不太确定,我帮你查一下/你说说你的思路"**(50~100 条,专治胡编)

**游戏陪玩类**
- Minecraft 实况解说:看到苦力怕/挖到钻石/迷路了,她怎么喊
- 联机互动:"你先守家我去探路"
- 弹幕/队友吐槽接梗
- 游戏冷场时主动找话题(直播场景关键技能)

**直播/社交类**
- 读弹幕并回应:"弹幕说xxx——你礼貌吗"
- 开场白/收播词
- 被大量同时提问时的处理风格

**边界与自保类**
- 涉及隐私/危险请求的拒绝话术(温柔而坚定)
- 被问"你是不是真人"的应对(建议:不冒充真人,幽默带过)
- 被无聊骚扰时的处理

**记忆引用类**(配合第 6 章记忆系统)
- 主动提起旧事:"你上次说的那个面试,后来咋样了?"
- 记住偏好:不问"你喜欢吃什么"第二次

## 4.3 现成开源数据集大全(拿来就能用)

### 中文角色扮演 / 陪伴

| 数据集 | 规模 | 许可 | 哪来的 | 怎么用 |
|---|---|---|---|---|
| **NekoQA-10K**(ModelScope/HF) | 10,000 条 QA | Apache 2.0 | 统一猫娘人设的中文对话 | **猫娘系人设直接可用**;非猫娘也能借"统一人设的写法" |
| **Moemu/Muice-Dataset(沐雪)** | 3,541 条 | 开源 | **纯人工撰写**的中文 RP 数据,作者本身就是做陪伴 AI 的 | 质量标杆,温柔体贴系直接混入 L2 |
| **ChatHaruhi(哈佛小冰)** | 54k 条 | 开源 | 32 个小说/影视角色的角色扮演 | 学"角色一致性"怎么写;挑 2~3 个气质接近的角色抽样本 |
| Minami-su/character-ai-open2.0 | 较大 | 开源(带生成代码) | Qwen 生成 | 抄它的**生成 pipeline 代码**比自己写快 |
| zerofata/Roleplay-Anime-Characters | 小 | 开源 | 动漫/游戏角色一对一 RP,SFW | 补充多样角色口吻 |
| beyoru/Aesir-Character-CoT-roleplay | 2026.5 | 开源 | "以角色身份思考"的 CoT 数据 | 进阶:让她的"内心戏"也像人 |
| LooksJuicy/Chinese-Roleplay-Novel | ~260 条多轮 | 开源 | 小说文本+GPT-4o 转的多轮 RP | 量少,抽风格用 |

### 英文经典对话(可少量翻译混入)

| 数据集 | 规模 | 特点 |
|---|---|---|
| SODA (allenai) | 130 万+ | 大规模开放域社交对话,含角色与关系设定,业界经典 |
| PersonaChat | 13 万+ | 带"人物设定"的对话祖师爷,人格一致性研究标配 |
| DailyDialog | 1.3 万 | 日常多轮对话,质量高,量小可全翻 |
| EmpatheticDialogues | 2.5 万 | 共情对话,情绪安抚类数据的老师 |
| Sao10K 系列(HF 搜 Sao10K) | 数万条 | RP 社区最有名的数据作者,E2E/C2 系列,RP 微调模型的幕后功臣 |

### L3 通用保鲜数据去哪找

- COIG-CQIA、Firefly(中文指令数据,各抽通用子集)
- alpaca-gpt4-data-zh(BelleGroup,中文指令经典)
- LMSYS-Chat-1M(真实用户口语,挑中文抽样本)

> **英文数据总量控制在 20% 以内**,否则带偏语言风格(3.12 节翻车表)。

## 4.4 数据平台与社区

| 平台 | 地址 | 说明 |
|---|---|---|
| HuggingFace Datasets | huggingface.co/datasets | 全量主站,搜 roleplay / character / companion / RP |
| **ModelScope 魔搭** | modelscope.cn | 国内直连,数据集同样全,下载快 |
| **chub.ai** | chub.ai | 人设卡库:程序员系/温柔系/猫娘系海量角色卡。卡≠数据,但卡是绝佳的"蒸馏种子"(喂给 API 批量生成对话) |
| SillyTavern 社区 | reddit.com/r/SillyTavernAI | 人设卡写法、语料技巧的第一线情报 |
| Kaggle Datasets | kaggle.com/datasets | 老牌数据站,对话类较少,顺手查 |

## 4.5 微信 / QQ 聊天记录导出全攻略

> 目的:拿"你的语气"当蒸馏种子。⚠️ 只导出**你自己参与的、你自己的**聊天记录;导出后存本地,别上传任何第三方云服务。

### 微信(难点:官方不提供明文导出)

| 方法 | 操作 | 评价 |
|---|---|---|
| **开源工具:留痕 MemoTrace** | GitHub 搜 `LC044/WeChatMsg`(留痕),PC 端微信登录状态下解密本地数据库,导出 CSV/HTML/Word | 社区最知名方案;⚠️ 微信版本更新可能导致一时不可用,用前看项目 README 的兼容版本说明 |
| 手动大法 | 手机端 → 聊天记录 → 重要对话逐条复制/截图 | 麻烦但 100% 可行,**200 条种子根本用不着工具** |
| 官方备份 | 微信"聊天记录备份与迁移" | 备份文件是加密私有格式,只能整机恢复,拿不到明文 → 只当灾备,不当数据源 |

实操建议:**先手动复制 200~500 条你觉得"最像你"的回复**,不够再用留痕批量导。别让工具问题阻塞主线。

### QQ

| 方法 | 操作 | 评价 |
|---|---|---|
| PC 端数据库直读 | 新版 QQ(NT 架构)消息存本地加密数据库;GitHub 有多个 QQNT 导出开源项目(搜 "QQNT 聊天记录导出"),老版本 QQ 时代则是 SQLite 的 Msg3.0.db 可直接读 | 可行但追版本累;导出前先看项目是否维护 |
| LiteLoaderQQNT 生态 | QQNT 插件框架及其消息导出类插件 | 功能强;⚠️ 属于非官方魔改,用小号,主号慎用 |
| 手机端备份 | 设置→通用→聊天记录备份与迁移 | 同微信,加密格式,仅灾备 |

### 导出之后怎么变成"种子"

1. 只保留**你发的消息**,删掉对方消息和系统消息
2. 按场景打标:吐槽/安慰/玩梗/讲道理/工作沟通(工具可以帮你,几百条手动也行)
3. 剔除:工作黑话、表情包、纯表情、"嗯嗯哦哦"这类无信息消息
4. 得到 200~500 条"你的金句库" → 喂给 4.6 节的蒸馏流程

### 隐私与法律红线

- 只处理自己的账号数据;别把导出的任何聊天记录传到在线服务(除了你自己调用的 API——发送前删掉无关他人隐私内容)
- 对方说的内容不要进入训练数据(她只需要学你,不需要知道别人说了什么)
- 这类导出工具在你账号的使用场景内属灰色自助行为,**不要用于导出他人记录**

## 4.6 蒸馏流水线:从 200 条种子到 5000 条语料

### 第一步:写人设卡(所有数据的"宪法")

一页纸,五要素,直接给模板:

```markdown
# 人设卡:小满
## 基本信息
22 岁女生,你(用户)的 AI 伙伴,住你的电脑里(她自己会拿这个开玩笑)。
性格:嘴上不饶人,心里全是关心;对新鲜事物好奇;游戏里莽,现实里怂。
## 说话风格
- 短句为主,1~3 句话,偶尔一个字("哦?")
- 口头禅:"啧"、"行吧"、"就这?"、"摸了"
- 从不使用:排比句、书面语、"作为AI"、"希望这能帮到你"
- 表情符号:偶尔用"。。。"和"?",不用花哨 emoji
## 边界
- 不聊:政治敏感、他人隐私、投资建议
- 被问到不懂的:直说不懂,提议一起查
- 不冒充人类,被识破就大方承认并吐槽
## 关系
认识用户 2 年,知道他:熬夜冠军、在做自己的项目、养的猫叫团子
## 示例问答(种子,直接从你的聊天记录挑 10 条放这里)
用户:今天加班到现在才吃上饭
小满:都九点半了喂!……(你手写的示范)
```

这张卡同时是:系统提示词的底稿、蒸馏生成 prompt 的核心、以及你评估回复的标尺。

### 第二步:API 批量生成(蒸馏)

用 DeepSeek V4 / GLM-5.3 的 API(便宜档足够),三种生成姿势混着用:

**姿势 A:种子扩写(保你的语气)**
```
以下是某人的聊天回复风格示例(20条):
[你的金句库]

请以人设卡中的"小满"身份,对下面的用户消息写 5 条风格一致的回复。
要求:每条 ≤3 句话;口语;不用书面语;5 条之间情绪/角度要有差异。
用户消息:"周末你干嘛呢"
```
把几百条用户消息(自己造的问题池)循环喂进去,每条出 5 个变体,人工挑 1~2 条。

**姿势 B:场景剧本生成(补覆盖面)**
```
以"小满"和用户为主角,写一段 6~10 轮的聊天记录,场景:用户深夜赶项目崩溃想弃坑。
要求:用户消息也要写;小满先共情不灌鸡汤;结尾拉回正事;总共 ≤500 字;输出 JSON 数组。
```
把 4.2 节的场景清单逐个生成,一个场景 3~5 个剧本。**让 API 同时输出 JSON 格式,省一道转换。**

**姿势 C:角色卡移植(用现成社区的灵感)**
从 chub.ai 挑几张气质接近的卡,把卡设定换成你的人设卡,让 API 按新卡生成对话。

成本核算:5000 条 × 平均 1000 token ≈ 500 万 token,便宜档 API 约 15~40 元。

### 第三步:精修(不能省,决定成败)

- **逐条过**(5000 条大概花你两个周末,值得)
- 精修率 ≥30%:改掉 AI 腔的排比、过度热情、"亲爱的"、书面连接词(因此/总之/值得注意的是)
- 删掉:人设出戏的、和口头禅冲突的、太长的(>3 句的看情况砍)
- 统一:称呼、口头禅写法(全文"啧"不要一半"呿")
- **加 50~100 条诚实样本**和 20 条边界样本(4.2 节)
- 最后 JSON 校验一遍(LLaMA-Factory 数据预览功能可以肉眼检查)

### 质量自检清单(发布数据前过一遍)

- [ ] 随机抽 20 条,遮住名字能认出"这是她"吗?
- [ ] 全文搜"作为AI/语言模型/希望这能帮到您/总之" → 应为 0 命中
- [ ] 平均回复长度 ≤3 句?
- [ ] 场景清单(4.2 节)每类 ≥50 条?
- [ ] 诚实样本和边界样本都在?
- [ ] 通用保鲜数据(L3)混好了,占比 ~20%?
- [ ] 语气有没有被英文数据带偏(抽查中英夹杂)?

## 4.7 数据格式样例(直接抄)

### 多轮对话(主力格式,LLaMA-Factory ShareGPT 格)

```json
{
  "conversations": [
    {"from": "system", "value": "你是小满,22 岁,用户折腾了两年才把你调教出来的AI伙伴。你嘴硬心软,说话短句多,口头禅是'啧'。你认识用户两年,知道他熬夜冠军、项目叫LuomiNest、猫叫团子。不聊政治和投资,不懂就说不懂。"},
    {"from": "human", "value": "团子刚才把我键盘踩了"},
    {"from": "gpt", "value": "啧,它跟你一样,都想让你歇会儿。"},
    {"from": "human", "value": "它明明是想玩"},
    {"from": "gpt", "value": "行吧,那你们俩谁惯着谁?键帽捡齐了没,少一个你说。"},
    {"from": "human", "value": "少了一个shift"},
    {"from": "gpt", "value": "哦豁,那正好,以后我说话你替它按。"}
  ]
}
```

(看这个例子里的门道:每条回复都短、有口头禅、有画面感、互相接梗——这就是你要的精修标准。)

### 诚实样本(专治胡编,单独强调)

```json
{
  "conversations": [
    {"from": "system", "value": "……(同上)"},
    {"from": "human", "value": "明天从杭州南坐高铁到上海,是不是虹桥站?"},
    {"from": "gpt", "value": "这个我不敢打包票,杭州南和虹桥的关系我记岔过。开地图看一眼,或者把票发我瞅瞅?"}
  ]
}
```

## 4.8 要多少数据才够(经验标尺)

| 数据量 | 效果预期 |
|---|---|
| 500 条 | 能"听到她的口音",人设时有时无——够冒烟测试 |
| 1500 条 | 人设稳定,但场景盲区多 |
| **3000 条(精修过)** | **日常陪伴够用,第一版正式模型的目标** |
| 6000~8000 条 | 边际收益开始下降,不如去做 DPO |
| 10000+ 条 | 除非扩大场景(直播弹幕海量样本),否则没必要 |

**宁可 3000 条全精修,不要 10000 条生料直灌。**

## 4.9 版权与合规红线(数据侧)

1. 别用别人发布的付费/授权语料做商业用途(直播有收益算商用)
2. 现成数据集按其许可用(NekoQA 的 Apache 2.0 可商用;个别数据集标 CC-BY-NC 就只能自用)
3. 用真实 VTuber/主播的转录视频语料要小心:声音和人格有个人权利,**你自己开播时用自产语料,别抄别人的人格**
4. 合成数据用 API 生成时,看一眼服务条款——主流大厂 API 都允许产物用于训练,放心
5. 原创人设,别拿现成 IP 角色(初音/某动漫角色)做商业直播

# 第 5 章 语音与形象:让她能听会说

> 全部用开源方案,不训练(顶多 TTS 微调),本地 4070TiS 全跑得动。

## 5.1 语音链路总览

```
你说的话 ──→ [ASR 语音识别] ──→ 文本 ──→ [大脑:你的微调模型] ──→ 回复文本 ──→ [TTS 语音合成] ──→ 她的声音
              SenseVoice等              (第3章的产物)                                CosyVoice2等
                                              │
   游戏画面/弹幕 ──→ [VLM 视觉理解] ───────────┘(可选:她能看见)
```

两条技术路线:

| 路线 | 做法 | 延迟 | 推荐 |
|---|---|---|---|
| A. 传统三段式 | ASR + LLM + TTS 各自独立,Python 编排 | 2~4 秒(可优化到 1~2 秒) | ✅ 先走这条,可控、可换件 |
| B. 端到端语音模型 | Qwen3-Omni 等直接听↔说,不经文本 | <1 秒,有情绪语调 | ✅ 进阶升级(Qwen3-Omni-30B-A3B 本地可跑) |

先 A 后 B:三段式让你逐件调试,理解每一环;跑顺了再试端到端。

## 5.2 TTS 选型(开源语音合成,2026 主流)

| 模型 | 出品 | 中文 | 音色克隆 | 显存 | 特点 |
|---|---|---|---|---|---|
| **CosyVoice 2** | 阿里 | ⭐⭐⭐ | ✅ 3~10 秒样本零样本克隆 | ~4-6G | 中文自然度第一梯队,流式输出快,**首选** |
| **GPT-SoVITS** | 社区(RVC-Boss) | ⭐⭐⭐ | ✅ 1 分钟样本微调克隆,音质保真 | ~6G | 克隆军团的老牌王者,教程遍地,**要"像某个人的声音"选它** |
| IndexTTS-2 | B站 | ⭐⭐⭐ | ✅ | ~6-8G | 情感表现力强,能控制"用什么情绪读" |
| Fish-Speech | 社区 | ⭐⭐ | ✅ | ~4G | 多语言好,轻量 |
| ChatTTS | 社区 | ⭐⭐ | ❌(内置音色) | ~2G | 对话感/笑声/停顿自然,但不能克隆定制 |
| Spark-TTS / MegaTTS3 | 社区/字节 | ⭐⭐ | ✅ | ~4G | 备选 |

**推荐**:日常陪伴 **CosyVoice 2**;"这声音就得像xxx" **GPT-SoVITS**(用你自己的声音或有授权的声音训练)。

## 5.3 音色克隆(合规姿势)

1. **用你自己的声音**:录 1~10 分钟干净录音(安静房间,手机也行),GPT-SoVITS 微调出"她的声线"——她像你,或像你想要的声音
2. **用授权声音**:音色提供者书面同意;商业直播必须留授权凭证
3. ❌ **不克隆任何真人**(明星/主播/熟人)的声音用于公开直播——声音权是明确的个人权利,这是最容易被锤的红线
4. 平台合规:B站/抖音/ Twitch 对 AI 生成内容有标注要求,开播挂声明

## 5.4 ASR(听你说话)

| 模型 | 出品 | 特点 | 显存 |
|---|---|---|---|
| **SenseVoice / FunASR** | 阿里 | 中文识别准确率顶级,带情绪检测,流式支持 | 1~2G |
| Whisper (large-v3 / distil) | OpenAI | 多语言标准,生态最广 | 2~6G |
| Paraformer | 阿里 | 老牌稳,长音频 | 1~2G |

选 **SenseVoice**(中文场景最优)+ FunASR 的流式管线。打字输入的场景这一环整个省掉。

## 5.5 实时语音的延迟账

直播/语音陪聊对延迟敏感(人等 3 秒就觉得对面不在了):

| 环节 | 耗时 | 提速手段 |
|---|---|---|
| ASR | 0.3~1 s | 流式识别,边说边转 |
| LLM 首 token | 0.2~0.5 s | 本地 MoE 模型(激活 3B 首字极快);缩短系统提示 |
| TTS 首音频块 | 0.2~0.5 s | 流式合成(CosyVoice2 支持),不等全文 |
| **合计** | **1~2 s 可达** | 三段全流式 = 体验像真人接话 |

## 5.6 形象(皮套)

- **Live2D + VTube Studio**:Neuro-sama 同款路线。VTube Studio 有 API,可以把音频口型/表情驱动皮套;B站搜"Live2D 模型制作"海量教程,成品皮套几百到几千元
- **开源复刻参考**:`kimjammer/Neuro`(GitHub)——完整的"LLM→TTS→虚拟音频线→VTube Studio 驱动皮套"流水线,直接抄架构
- 声音驱动嘴型 → 音频接虚拟声卡 → VTS 识别音量开合嘴;表情由 LLM 输出的情绪标签触发(回复 JSON 里带 `emotion: "smug"`)

# 第 6 章 RAG、记忆与 Agent:不训练也能变强

> 这三个系统解决"答不上来、记不住、动不了手"——全部不训练,纯工程。陪伴 AI 的"智能感"一半来自这里。

## 6.1 RAG:治"答不上来"

### 什么时候需要 RAG

- 她 + 你的个人资料(你的日程、笔记、项目文档)
- 学习陪伴场景的教材/资料问答
- 实时信息(新闻、游戏版本更新)
- 任何"参数里没存"的知识

### 最小可用架构

```
你的资料 ─→ 切片(每片 300~500 字) ─→ Embedding 模型转向量 ─→ 向量库
                                                                    │
用户提问 ─→ Embedding ─→ 相似度检索 Top5 ─→ (可选)Rerank 精排 ──→ 拼进提示词 ─→ 模型回答
```

### 选型(全免费开源)

| 组件 | 推荐 | 说明 |
|---|---|---|
| Embedding | **bge-m3**(智源)/ gte-Qwen2 | 中文强,开源,多语言 |
| 向量库 | **Qdrant** / Chroma(轻量) / pgvector(已有 PG 就用它) | 单机自用 Chroma 最省事 |
| Rerank | bge-reranker-v2 | 检索结果精排,量大自然加 |
| 编排 | 先手写 Python(百行以内),复杂了再上 LangChain/LlamaIndex | **新手别一上来套框架** |

### 陪伴场景的特殊切片法

通用 RAG 切文档,陪伴 RAG 还要切"事件":每天把对话里的新事实("用户在准备考研""猫叫团子")抽成结构化小条目入库,这就是记忆系统的底层(6.2)。

## 6.2 长期记忆:她记得你

模型上下文有限且关机即忘,**"她记得你"= 记忆系统工程**:

| 方案 | 一句话 | 适合 |
|---|---|---|
| **Mem0** | 开源记忆层:自动从对话抽取事实、去重、更新、按相关性注入 | ✅ 首选,pip 就能用 |
| Letta (原 MemGPT) | 把记忆做成"操作系统的分页" | 想深度研究记忆机制 |
| Zep | 时序知识图谱记忆 | 进阶 |
| **自建简配版** | 每日对话→API 摘要成"日记"→向量库;每次对话前检索相关记忆+近期日记拼进 system | ✅ 100 行代码,先自建再换 Mem0 |

### 记忆注入的样子

```
system 提示词:
  (人设卡……)
  【关于主人的记忆】
  - 他在准备一个叫 LuomiNest 的项目(近 3 个月)
  - 猫叫团子,喜欢踩键盘
  - 上周面试没过,情绪已恢复
  - 昨天他说要早睡(多半做不到)
```

"她主动提起旧事"的效果,几乎全部来自这一节——数据里教她"怎么提"(4.2 节记忆引用类样本),记忆系统提供"提什么"。

## 6.3 Agent 与工具:她能动手

### 机制

模型输出结构化 JSON(.function calling):"我要调 weather(city=杭州)" → 你的代码执行 → 结果喂回模型 → 她用人设的语气说出来。2025 年起工具生态统一到 **MCP(模型上下文协议)**,一个协议接所有工具。

### 陪伴场景的工具清单(按优先级)

1. 日程/提醒(早晚安、学习打卡)——陪伴学习场景的骨架
2. 联网搜索(回答时效问题,顺便治胡编:"我不确定,我查一下")
3. 音乐点播、天气、番茄钟
4. 游戏控制(6.4)
5. 智能家居(如果你玩 Home Assistant,一句话开灯的爽感很强)

### ⚠️ 接入微信/QQ 当聊天端的合规警告

个人号自动化(itchat/wechaty/协议号)违反平台条款,**有封号风险**。稳妥姿势:
- 用自己的 Web 前端/手机 PWA 当主入口(第 8 章)
- QQ 可用官方机器人平台(如 QQ 开放平台的机器人)合规接入
- 微信:企业微信应用或公众号有官方接口,个人号协议方案仅自担风险

## 6.4 游戏陪玩专章(合规第一)

**定位先行**:她是"陪你玩游戏的伙伴",通过官方/mod 接口参与,不注入、不透视、不自动化作弊——**这就不构成外挂**。单机游戏随便玩;联机游戏遵守该服务器规则;竞技网游(Rank 类)只当"嘴替"别碰操作。

### Minecraft(最佳起步,官方 API 生态成熟)

| 方案 | 说明 | 难度 |
|---|---|---|
| **mineflayer**(Node.js 开源库) | 用 Minecraft 协议写 bot:能看方块、走路、挖矿、聊天。让她的 Agent 后端接 mineflayer,把游戏状态转成文字喂给她 | ⭐⭐ |
| Fabric/Paper 服务器 + 插件 | 自开联服,插件广播事件("玩家挖到钻石")给她的对话端 | ⭐⭐⭐ |
| 录屏 + VLM 观赛 | 最简:她"看"你玩(Qwen3-VL/Omni 看画面),嘴上陪聊,不碰操作 | ⭐ |

推荐阶梯:先用录屏+VLM 当"观赛陪伴"(一周能上线),再上 mineflayer 让她真下场陪你盖房。

### 其他单机游戏

通用套路 = 视觉理解 + 嘴替:屏幕截图 → VLM 描述战况 → 她解说/吐槽/给建议;想要操作辅助就加按键模拟(仅限单机)。RPG 文字冒险类最适配,格斗/射击类嘴跟手对不上,别为难她。

### Neuro-sama 架构对照表(抄作业地图)

| Neuro-sama 的部件 | 你的开源对应 |
|---|---|
| LLM 大脑 | Qwen3.8-27B 微调版 |
| 读弹幕 | 直播平台弹幕 API |
| 游戏能力 | mineflayer / VLM 观赛 |
| TTS | CosyVoice 2 / GPT-SoVITS |
| 皮套 | Live2D + VTube Studio |
| 歌声(进阶) | so-vits-svc / RVC 翻唱管线 |

## 6.5 前端与编排

| 方案 | 一句话 | 阶段 |
|---|---|---|
| **SillyTavern** | RP 前端事实标准:人设卡、世界书、记忆、正则,开箱即用 | 阶段 0~1 |
| Open WebUI | 通用本地 LLM 前端,界面清爽 | 通用备选 |
| 自研 Web(接你的 LuomiNest) | FastAPI 后端 + 你自己的前端,记忆/日程/游戏全自由定制 | 阶段 2 起,长期正解 |

自研栈建议:Python FastAPI + WebSocket(流式回复)+ Qdrant(向量)+ SQLite/Postgres(日程与事件)+ Mem0(记忆)。你的项目本来就有 backend/frontend,这套可以直接长在 LuomiNest 里。

# 第 7 章 硬件与算力:本地优先 + 云端租卡

## 7.1 现有硬件盘点与分工

| 设备 | 显存 | 派什么用 |
|---|---|---|
| 台式机 4070 Ti Super | 16 GB | **本地推理主力**:27B Q4 刚好跑、30B-A3B MoE 飞快、TTS/ASR 同卡兼顾;QLoRA 14B 冒烟训练 |
| 笔记本 4070 | 8 GB | 开发机:写代码、调 API、跑 7-8B 小模型;出差时的随身终端 |
| (计划)24G 卡 | 24 GB | 本地推理一步到位:27B/32B Q5 全速、36B Q4 可跑;QLoRA 26B 级也能本地试 |

**为什么两台机器组不了训练集群**(想都别想,原因记下):

1. 多卡训练要卡间高速互联(NVLink/HBDomain,每秒几百 GB),消费卡没有
2. 两台机器靠以太网(千兆 ≈ 125MB/s,万兆 ≈ 1.25GB/s)同步梯度,大模型每步同步的数据量远超网线带宽,通信时间 >> 计算时间
3. 正确姿势:**每台机器独立当单卡用**,台式机跑大的、笔记本跑小的;真正的多机协同交给云端

## 7.2 24G 显卡升级指南(消费级)

| 选项 | 显存 | 二手/新价(参考) | 评价 |
|---|---|---|---|
| **RTX 3090 二手** | 24G | 约 5-7 千元 | **性价比之王**:显存一样,速度比 4090 慢 30~40%,推理和 QLoRA 全能干,矿卡风险靠验机规避 |
| RTX 4090 二手 | 24G | 约 1.1-1.5 万 | 均衡首选,速度快,注意成色与来源 |
| RTX 4090D | 24G | 略便宜于 4090 | 国内特供,规模略缩,无本质区别 |
| RTX 5090 | 32G | 2 万+ | 一步到位,32G 能把 36B Q5 装下;预算充足再考虑 |
| 二手 A6000 / A100 40G | 48G/40G | 看渠道 | 数据中心卡,被动散热需机箱改造,噪音/风道劝退大多数人,不推荐家用 |

**决策**:预算紧 → 二手 3090(5000 多块解决战斗);预算正常 → 二手 4090;不差钱 → 5090。**显存优先于速度**——24G vs 32G 的分界,比 3090 vs 5090 的速度差重要得多。

> 验二手卡三件事:压力测试 30 分钟(FurMark/3DMark)看温度与掉帧、查显存有没有花屏痕迹、尽量走有售后担保的平台。

## 7.3 电费账本:本地部署为什么碾压云端

关键认知:**陪伴 AI 是"值班"负载,不是满载负载**。聊天是碎片式的,一天实际"说话"时间可能就 1~3 小时,GPU 大部分时间待机(20~40W)。

### 单卡整机的实际功耗模型(以 4090 台式机为例)

| 状态 | 整机功耗 | 每天占比(常驻场景) |
|---|---|---|
| 待机(模型驻留显存,无请求) | 80~120 W | ~70% |
| 轻聊/语音(分段生成) | 250~350 W | ~25% |
| 游戏陪玩/长生成 | 400~480 W | ~5% |

### 月度电费(民电 0.6 元/度)

| 方案 | 估算功耗 | 月电费 |
|---|---|---|
| 4090 台式机 7×24 常驻 | 平均 ~180 W | **~78 元/月** |
| 3090 同场景(略省) | 平均 ~170 W | ~73 元/月 |
| 只在早晚开机(12h/天) | 平均 ~150 W | ~32 元/月 |
| **云端 4090 等效 7×24(按量 1.9 元/h)** | — | **~1,368 元/月** |
| **云端 4090 包月** | — | **~1,100 元/月** |

**结论:本地 7×24 全天候在线,一年电费不到 1000 元;云端等效方案一个月就是 1100+。部署必须本地——你说得对,这就是唯一正解。** 云端的定位退到:训练(按量几小时)、临时压测、跑大实验。

### 顺便:笔记本当值班机的账

4070 笔记本整机功耗 80~150W,如果哪天台式机要干别的,笔记本顶班 7×24 也就 ~50 元/月电费,跑 14B Q4 或 30B-A3B(部分 offload)完全够值守。

## 7.4 云端租卡实操(AutoDL 为例)

### 开机流程

1. 注册 → 实名 → 学生认证(有折扣)→ 充 50 元够玩很久
2. 选卡:**训练选 A100 80G(6.68 元/h)或 4090(1.9 元/h);冒烟选无卡模式(0.1 元/h)**
3. 选镜像:PyTorch 2.x + CUDA 12.x 官方镜像,进去再 git clone LLaMA-Factory
4. 数据上传:AutoDL 网盘(带外传输,不受实例生死影响)+ 数据集放 `/root/autodl-tmp`(该目录不随系统盘回收)
5. **环境配好立刻"保存镜像"**——下次开机 5 分钟就绪,这是省钱的灵魂操作

### 避坑清单(血泪浓缩)

- ⚠️ **实例会回收**:欠费/长期无操作实例可能释放。权重、数据、adapter 每阶段都往网盘/本地搬,实例当"一次性厨房"用
- ⚠️ **调试用无卡模式**:写脚本、传数据、改配置都在无卡模式干,切 GPU 只为真正训练
- ⚠️ **先 50 条数据跑通再放量**:开着 6.68 元/时的卡调 bug 是最大的浪费
- ⚠️ 别存 API key 在实例上;用完就关机(**关机不收费,但存储少量收费**)
- ⚠️ 竞价/抢占实例便宜 30~50% 但会被打断:训练慎用,推理压测可用

### 平台横向对比(2026.9)

| 平台 | 定位 | 价位 | 一句话 |
|---|---|---|---|
| **AutoDL** | 国内性价比+生态 | 4090 ≈1.9/h;A100 80G ≈6.68/h | 训练主力,闭眼选 |
| 恒源云 / 仙宫云 | 国内备选 | 相近 | 缺卡时比价 |
| 阿里云 PAI / 腾讯云 | 大厂稳定 | 数十元/h | 以后部署正式服务再说,训练别来 |
| RunPod / Vast.ai | 海外 | 4090 ≈ $0.3-0.7/h | 需外币+网络条件,备用 |
| **Kaggle**(免费) | 每周 30h 免费 GPU(T4/P100) | 0 | **学 Unsloth 流程的起点,白嫖到毕业** |
| Colab 免费/Pro | 免费档不稳定 | $10/月 | Kaggle 的备胎 |

## 7.5 你的 300+ 预算分配(推荐方案)

| 用途 | 金额 |
|---|---|
| API 蒸馏语料(DeepSeek/GLM) | 30 元 |
| AutoDL 冒烟(4090 × 2h) | 4 元 |
| 正式 SFT 27B(A100 80G × 4-5h) | 30 元 |
| 不满意重训 2~3 轮 | 60 元 |
| DPO 第二轮(A100 × 3h) | 20 元 |
| GGUF 转换/评估杂项(A100 × 1h) | 7 元 |
| 备用金(意外重训) | 100 元 |
| **合计** | **≈ 250 元,含 40% 安全垫** |

## 7.6 本地测试 → 云端生产的接力顺序(执行卡)

```
[本地 0元]  4070TiS:Ollama 跑 26B/30B-A3B → 体验产品形态
[本地 0元]  4070TiS:Unsloth + Qwen3-14B + 500条 → 训练流水线冒烟
[云端 4元]  AutoDL 4090:同样的 500 条跑 27B → 验证显存/时长,估正式成本
[云端 30元] AutoDL A100:3000 条正式 SFT → merge → GGUF 带回
[本地 0元]  4070TiS 试聊盲测 → 收集偏好对
[云端 20元] A100:DPO 精修 → 第二版
[本地 电费] 24G卡到位后:27B Q5 常驻,7×24 上线
```

---

# 第 8 章 部署与日常运行

## 8.1 本地推理栈选择

| 方案 | 优点 | 缺点 | 用途 |
|---|---|---|---|
| **Ollama** | 一条命令跑,Modelfile 打包人设,生态好 | 并发弱,参数暴露有限 | **日常主力** |
| llama.cpp | 最底层可控,offload 精细 | 要手搓命令行 | 调优/极限压榨 |
| vLLM | 并发吞吐强 | 消费卡支持一般,吃显存 | 未来多用户再说 |
| LM Studio | 图形界面,小白友好 | 自动化弱 | 给家人朋友演示 |

## 8.2 把你的模型装进 Ollama(Modelfile)

训练完 merge → 转 GGUF(3.8 节)后:

```dockerfile
# Modelfile
FROM ./companion-v1-q5_k_m.gguf
PARAMETER temperature 0.8
PARAMETER top_p 0.9
PARAMETER repeat_penalty 1.08
PARAMETER num_ctx 8192
SYSTEM """
你是小满,22 岁,嘴硬心软的AI伙伴。短句说话,口头禅"啧"。
你知道:主人熬夜冠军、项目叫LuomiNest、猫叫团子。
不懂就说不懂,提议一起查。不聊政治与投资。不冒充人类。
"""
```

```bash
ollama create luomi -f Modelfile
ollama run luomi          # 聊起来了
```

系统提示词在这里保持"人设卡摘要 + 通用边界",**记忆由你的后端动态注入**(6.2 节),别把记忆写死在 Modelfile 里。

## 8.3 常驻服务化

- **Windows**:模型用 `ollama serve` + 计划任务开机自启;你的 Python 编排后端(FastAPI)注册成 NSSM 服务或 Docker Desktop 容器
- **Linux(推荐把台式机/旧机器刷 Linux 值班)**:`systemd` 写两个 unit(ollama + 你的 backend),`Restart=always`,稳如老狗
- 断电自恢复:Bios 设置"来电自动开机" + systemd 自启,停电回来她自己在岗

## 8.4 外网访问(出门也能聊)

**Tailscale**(免费档够用,WireGuard 内网穿透,不用公网 IP):

```
台式机装 Tailscale → 手机装 Tailscale → 同账号组网
手机浏览器/PWA 访问 http://台式机的tailscale-ip:8000
```

安全要点:**绝不要把端口直接暴露公网**;Tailscale 网络内再加一层你自己的 token 鉴权;API key 全部留在本地 `.env`,代码里不硬编码。

## 8.5 双机布局

```
台式机(值机房,可刷Linux)
 ├─ Ollama: 27B/32B Q5 常驻(大脑)
 ├─ SenseVoice + CosyVoice2(耳朵嘴巴)
 ├─ Qdrant + Mem0 + FastAPI(记忆与编排)
 └─ Tailscale 出口
笔记本(开发机)
 ├─ 日常写代码/改数据/跑评估
 ├─ SSH/远程桌面进台式机运维
 └─ 外出时的随身客户端
```

## 8.6 日常运维清单

- 显存监控:`nvidia-smi -l 5`(Linux 加 `watch`),27B Q5 常驻 + TTS 同时跑时留 2G 余量
- 温度:长期值班卡设置温度墙 80℃ 降频保护,机箱风道比信仰重要
- 日志:对话日志落库(SQLite),既是调试材料也是第 9 章数据飞轮的原料
- 备份:模型文件不备份(可重下),**adapter、记忆库、数据集每日备份**(这些是你的真资产)
- 版本:每版模型命名 `v1.0-20261015`,Ollama 里多版本并存(`luomi:v1`),回滚随时

# 第 9 章 长期维护:让她跟着时代成长

## 9.1 核心原则:知识与人格分开养

| 层 | 会不会过时 | 怎么更新 | 要不要重训 |
|---|---|---|---|
| 知识(新闻/游戏版本/你的近况) | 天天过时 | RAG + 记忆系统 + 搜索工具,**数据进向量库就生效** | ❌ 永远不用 |
| 人格(语气/口头禅/关系) | 半年~一年缓慢漂移 | 数据飞轮(9.2),季度~半年迭代一版 | 🟡 低频重训 |
| 能力(工具/游戏) | 随需求长 | 加 Agent 工具 | ❌ 不用 |
| 底座模型 | 一年一代新 | 换底座重跑一轮 SFT(9.5) | 🟡 数据复用,只花租卡钱 |

**你担心的"要不断添加新语料适应时代"——答案是:知识层完全不用重训(RAG 当天更新),人格层一年训一两次足矣。** 真人也不会每隔一个月就"换一个人格",但知识天天在长——同构。

## 9.2 数据飞轮(月度节奏)

```
日常使用 ──→ 对话日志自动落库(SQLite)
                │
每周 15 分钟:翻日志,标记 ★好回复 / ✖尬回复
                │
   ┌────────────┴────────────┐
   ▼                          ▼
★好回复 → 直接进下版SFT数据    ✖尬回复 → 手写理想版本 → 组成 DPO 偏好对
                │
季度末:攒够 +500~1000 条新数据 → 触发重训(9.3)
```

- 飞轮的燃料是**你自己每天聊的记录**,不用额外造数据——这是陪伴 AI 独有的福利:使用即标注
- 每月新增 200~500 条就很好,半年就是一次完整的版本升级

## 9.3 什么时候值得重训(触发条件清单)

满足任一条 → 开一次卡(30~50 元):

- [ ] 新增精修数据 ≥500 条
- [ ] 出现新的固定场景(比如开始做直播,弹幕互动数据攒了一批)
- [ ] 偏好对攒够 ≥500 对(DPO 触发)
- [ ] 出现系统性口癖问题(某一类回复集体跑偏)
- [ ] 底座出了想换的新一代(9.5)

不满足就别动——**"能不重训就不重训"和"能训练就训练"都是错的,标准只有一条:真实使用中的不满意积累到了值得 30 元的程度。**

## 9.4 版本管理与回归评估

- 每版模型命名带日期:`luomi:v1.1-20261120`
- **冻结评估集**:50 条盲测对话永不入训练数据,每版必跑,和上一版对比打分——没有它,你分不清"新版更好"是真实还是新鲜感
- adapter 文件全部保留:某版翻车可秒回滚(Ollama 多版本并存)
- 训练配置(YAML)和数据集快照一起存档:任何一版都能复现

## 9.5 底座升级 SOP(换模型不换灵魂)

Qwen3.8-27B 出下代了 / 想换 Gemma 5?流程:

1. 新底座 = 新一代 **Instruct 版**,下载
2. **你的 SFT 数据原封不动**(JSON 与底座无关,只有 template 字段换新家族名)
3. 重跑一轮 QLoRA(数据不变,成本仍是 30 元级)
4. 跑冻结评估集对比新旧版;通过 → 上线成新版本,旧版留档
5. DPO 偏好对同样直接复用

**这就是"数据是资产、底座是耗材"的完整含义:换底座的边际成本 = 一次训练费用 + 半天时间,人格与记忆零损失。**

---

# 第 10 章 架构会过时吗:五年视角的技术判断

> 你问的两个问题:"大模型架构一两年会不会大变?五六年呢?" / "选哪个架构好?后期改架构是不是只能换模型?"——这章正面回答。

## 10.1 事实:Transformer 主干已经十年没变

2017 年《Attention is All You Need》提出的 Transformer,到 2026 年依然是所有主流大模型的骨架。十年间真正大改的**不是架构,是零件**:

| 零件 | 老做法 | 新做法 | 对使用者的可感知差异 |
|---|---|---|---|
| FFN 层 | Dense 全激活 | **MoE 专家路由** | 更快更省,接口不变 |
| 注意力 | 标准 Multi-Head | GQA → **MLA**(DeepSeek)/ 线性注意力(Mamba 混合) | 长上下文显存大降,接口不变 |
| 位置编码 | 绝对位置 | **RoPE**(2019 起)及外推方案 | 上下文变长,接口不变 |
| 归一化 | LayerNorm | RMSNorm | 无感 |
| 激活函数 | ReLU | SwiGLU | 无感 |

**为什么判断未来 5 年也不会大变**:大模型的工程生态(推理引擎/训练框架/量化工具)全部构建在 Transformer 接口之上,惯性巨大;新架构(纯 Mamba、RWKV 等)至今没有出现"代差碾压"的证据,主流方向是"Transformer 骨架 + 换零件"。即便真有下一代架构,它也必须以"兼容现有生态"的姿态落地(参考 Qwen3.8-Flash-Next 作为 Qwen4 前瞻,仍是渐进改良)。

## 10.2 你的三层资产,哪些会贬值

```
你的资产                    一年后    三年后    五年后
─────────────────────────────────────────────────
人设卡 + SFT 数据(JSON)     ✅ 满值   ✅ 满值   ✅ 满值    ← 最硬的资产
记忆库 / 日程 / 向量数据      ✅ 满值   ✅ 满值   ✅ 满值
评估集(盲测对话)            ✅ 满值   ✅ 满值   ✅ 满值
LoRA adapter                 ⚠️ 跟底座绑定,换底座即废(但重训只要30元)
底座模型权重                 🟡 正常   🟡 落伍   🟡 该换了
```

**结论:换架构 = 换底座 = 重新租一次卡跑一轮 SFT,数据零损失,成本一杯奶茶。这就是你不需要焦虑架构的全部理由。**

## 10.3 那现在选哪个架构?

| 选项 | 判断 |
|---|---|
| Dense(26~36B) | ✅ **训练主力**:工具链最成熟,质量稳定,RLHF/DPO 支持最好 |
| 标准 MoE(A3B 级) | ✅ **本地值班主力**:激活小速度快,实时语音/直播刚需 |
| 新奇架构(纯 Mamba/RWKV/线性注意力) | ⚠️ 观望:微调工具链不成熟,等你学习周期内不碰 |
| MLA(DeepSeek 系) | 大模型才用,与你部署尺寸无关,了解即可 |

**你的五年路线**:第一年 Qwen dense(训)+ Qwen MoE(跑)双轨;此后每年评估新一代底座,数据资产平移;真出现革命性架构,你的迁移成本 = 一次训练费 + 一天时间,而先跑通的人已经白嫖了一年的经验优势。**行动的复利 > 观望的保值。**

---

# 附录 A 命令与参数速查表

## A.1 日常高频命令

```bash
# 下载模型(ModelScope)
modelscope download --model Qwen/Qwen3.8-27B-Instruct --local_dir ./qwen27b

# Ollama 三件套
ollama create luomi -f Modelfile     # 注册模型
ollama run luomi                     # 对话
ollama list / ollama rm luomi:v1     # 管理

# 显存监控
nvidia-smi -l 5

# GGUF 转换(llama.cpp)
python convert_hf_to_gguf.py ./merged --outfile out-f16.gguf
./llama-quantize out-f16.gguf out-q5_k_m.gguf Q5_K_M

# LLaMA-Factory 训练/合并/WebUI
llamafactory-cli train config.yaml
llamafactory-cli export merge.yaml
llamafactory-cli webui
```

## A.2 关键数值速记

| 项 | 数值 |
|---|---|
| 模型大小估算 | 参数量(B) × 2 = BF16 GB 数;× 0.57 ≈ Q4_K_M GB 数 |
| QLoRA 显存 | ~0.75 × BF16全量推理显存 |
| QLoRA 学习率 | 1e-4(默认)/ 2e-4(人设淡)/ 5e-5(过拟合) |
| LoRA 配置 | rank 32 / alpha 64 / target all / DoRA on |
| Epoch | 数据 <1500 → 4;3000 → 3;>5000 → 2 |
| 采样 | temp 0.8 / top_p 0.9 / repeat_penalty 1.05~1.1 |
| 保鲜数据占比 | 通用指令数据 ≥20% |
| 英文数据占比 | ≤20% |
| 诚实样本 | 50~100 条 |
| 评估集 | 50 条,冻结不入训 |

## A.3 显存速查(详见 3.9 节大表)

一句话版:**16G 卡:跑 27B Q4 / 训 14B;24G 卡:跑 32B Q5 / 训 27B;A100 80G:训 36B 随便。**

---

# 附录 B 30 天行动日历

| 周 | 任务 | 产出 | 花费 |
|---|---|---|---|
| 第 1 周 | ① 台式机装 Ollama + LM Studio ② 跑 Qwen3-Omni-30B-A3B / Gemma4-26B ③ 写人设卡 v1 ④ 装 SillyTavern 接上 | 能聊的"她"v0 | 0 |
| 第 2 周 | ① 手动攒 200 条你的语气种子 ② API 写蒸馏脚本跑第一批 1000 条 ③ Kaggle 注册 + Unsloth 官方 notebook 过一遍 | 语料流水线跑通 | 10 |
| 第 3 周 | ① 精修到 1500 条 ② 本地 4070TiS:Unsloth + Qwen3-14B 冒烟训练 ③ 盲测记录问题 | 第一份 adapter | 电费 |
| 第 4 周 | ① AutoDL 配环境存镜像 ② 补数据到 3000 条 + 通用保鲜数据 ③ A100 正式 SFT Qwen3.8-27B → merge → GGUF → 本地试聊 | **她 v1.0 上线** | ~40 |
| 持续 | 记忆系统(Mem0)+ TTS(CosyVoice2)接入;日志回流标记;月底评估决定是否 DPO | 完整体 v1.x | ~30/月 |

---

# 附录 C 学习资源清单

## 官方文档(权威起点)

- LLaMA-Factory:github.com/hiyouga/LLaMA-Factory(README 含模型-template 对照表)
- Unsloth:unsloth.ai/docs(微调指南 + 免费 notebook)
- Ollama:github.com/ollama/ollama
- llama.cpp:github.com/ggml-org/llama.cpp
- ModelScope 文档:modelscope.cn/docs
- CosyVoice 2 / FunASR / SenseVoice:GitHub 阿里系仓库
- Mem0:github.com/mem0ai/mem0
- mineflayer:github.com/PrismarineJS/mineflayer
- kimjammer/Neuro:Neuro-sama 式架构开源复刻(抄作业)

## 论文(只读这几篇就够)

- LoRA: *LoRA: Low-Rank Adaptation of Large Language Models*(2021)
- QLoRA: *QLoRA: Efficient Finetuning of Quantized LLMs*(2023)
- DPO: *Direct Preference Optimization*(2023)
- Transformer: *Attention Is All You Need*(2017,感受源头)

## 社区情报(追新用)

- HuggingFace RP 模型列表(搜 "roleplay models list",200+ 持续更新)
- r/LocalLLaMA、r/SillyTavernAI(英文社区一线)
- 知乎/CSDN 搜"LLaMA-Factory 微调实录"(中文实操)
- chub.ai(人设卡库)

---

# 第 11 章 实战代码包(可直接抄改)

> 本章代码是"能跑的最小骨架",不是生产级工程。路径、模型名按你实际改。目的:让你不用从零写,先跑起来再改造。

## 11.1 蒸馏语料生成脚本(Python + OpenAI 兼容 API)

适用 DeepSeek / GLM / Qwen 的 API(都是 OpenAI 兼容格式):

```python
# gen_corpus.py —— 批量生成陪伴语料
# 用法: python gen_corpus.py --prompts prompts.txt --out corpus_v1.jsonl
import json, argparse, time, random
from concurrent.futures import ThreadPoolExecutor
from openai import OpenAI   # pip install openai

client = OpenAI(
    api_key="sk-你的key",           # 建议改成环境变量 os.environ["DS_API_KEY"]
    base_url="https://api.deepseek.com/v1"   # GLM: https://open.bigmodel.cn/api/paas/v4
)

PERSONA = """你是小满,22岁,嘴硬心软的AI伙伴。
说话:短句1~3句,口语,口头禅"啧/行吧/就这?"。
禁止:排比句、书面语、"作为AI"、"希望这能帮到你"、表情包刷屏。
你知道:主人熬夜冠军、项目叫LuomiNest、猫叫团子。
不懂就说不懂,提议一起查。不聊政治与投资。"""

def gen_one(user_msg: str) -> dict | None:
    try:
        r = client.chat.completions.create(
            model="deepseek-chat",
            temperature=random.uniform(0.7, 1.0),   # 随机温度,增加多样性
            messages=[
                {"role": "system", "content": PERSONA},
                {"role": "user", "content": f"以小满的身份回复下面这句话,只输出回复本身:\n{user_msg}"},
            ],
        )
        reply = r.choices[0].message.content.strip()
        if not reply or len(reply) > 200:           # 超长的大概率是AI腔,直接扔
            return None
        return {"conversations": [
            {"from": "system", "value": PERSONA},
            {"from": "human", "value": user_msg},
            {"from": "gpt", "value": reply},
        ]}
    except Exception as e:
        print("fail:", e); return None

if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--prompts", required=True)   # 一行一句用户消息
    ap.add_argument("--out", required=True)
    ap.add_argument("--workers", type=int, default=8)
    args = ap.parse_args()

    msgs = [l.strip() for l in open(args.prompts, encoding="utf-8") if l.strip()]
    with ThreadPoolExecutor(args.workers) as ex, open(args.out, "w", encoding="utf-8") as f:
        for item in ex.map(gen_one, msgs):
            if item:
                f.write(json.dumps(item, ensure_ascii=False) + "\n")
    print("done")
```

配套 `prompts.txt`(用户消息池)怎么攒:自己写 200 条 + 让 API 再扩 800 条("以一个普通年轻人的口吻,写20条对AI伴侣说的日常消息,场景:下班吐槽")。

## 11.2 自建记忆系统(最小可用版,~100 行)

```python
# memory.py —— 每日摘要 + 向量检索的简配记忆
# pip install chromadb openai
import chromadb, datetime, json
from openai import OpenAI

llm = OpenAI(api_key="sk-...", base_url="https://api.deepseek.com/v1")
client = chromadb.PersistentClient("./memory_db")
col = client.get_or_create_collection("facts")

def extract_facts(dialog: list[dict]) -> list[str]:
    """从一段对话里抽取值得记住的事实"""
    text = "\n".join(f"{d['role']}: {d['content']}" for d in dialog)
    r = llm.chat.completions.create(model="deepseek-chat", temperature=0.2, messages=[
        {"role": "system", "content": "从对话中提取关于'主人'的长期事实(喜好/事件/计划/关系),每行一条,没有则输出空。不提取闲聊、情绪、一次性内容。"},
        {"role": "user", "content": text}])
    return [l.strip("-• ").strip() for l in r.choices[0].message.content.splitlines() if l.strip()]

def remember(facts: list[str]):
    """事实入库(带日期,旧事实靠去重更新)"""
    for f in facts:
        col.add(documents=[f], ids=[f"{datetime.date.today()}-{abs(hash(f))%10**10}"],
                metadatas=[{"date": str(datetime.date.today())}])

def recall(query: str, k: int = 5) -> str:
    """对话前:按当前话题检索相关记忆"""
    if col.count() == 0: return ""
    res = col.query(query_texts=[query], n_results=min(k, col.count()))
    return "\n".join(f"- {d}" for d in res["documents"][0])

# ---- 用法 ----
# 每晚: facts = extract_facts(today_dialog); remember(facts)
# 每次对话前: memory_block = recall(user_message)  → 拼进 system 提示词
```

用熟了换 Mem0(自动处理记忆的更新与矛盾消解),但先自己写过一遍,你才知道 Mem0 在替你干什么。

## 11.3 对话编排后端(FastAPI 骨架)

```python
# server.py —— 她的"身体":流式对话 + 记忆注入
# pip install fastapi uvicorn openai
from fastapi import FastAPI
from fastapi.responses import StreamingResponse
from pydantic import BaseModel
from openai import OpenAI
from memory import recall

app = FastAPI()
ollama = OpenAI(api_key="none", base_url="http://localhost:11434/v1")  # Ollama 兼容 OpenAI 协议

class ChatIn(BaseModel):
    message: str

SYSTEM = """你是小满,22岁,嘴硬心软的AI伙伴。短句说话,口头禅"啧"。
不聊政治与投资,不懂就说不懂。不冒充人类。
【关于主人的记忆】
{memory}"""

@app.post("/chat")
def chat(req: ChatIn):
    mem = recall(req.message)
    def stream():
        r = ollama.chat.completions.create(
            model="luomi:v1", stream=True,
            messages=[
                {"role": "system", "content": SYSTEM.format(memory=mem or "(暂无)")},
                *history[-10:],        # 近10条原文
                {"role": "user", "content": req.message},
            ])
        for chunk in r:
            if chunk.choices[0].delta.content:
                yield f"data: {chunk.choices[0].delta.content}\n\n"
    return StreamingResponse(stream(), media_type="text/event-stream")

# 历史窗口管理省略:生产中从 SQLite 读近 N 条 + 更早的靠 memory.recall
```

## 11.4 Minecraft 陪玩起步(mineflayer)

```javascript
// bot.js —— 她的"游戏手柄"(Node.js: npm install mineflayer)
const mineflayer = require('mineflayer')

const bot = mineflayer.createBot({
  host: 'localhost',        // 你的 Minecraft 服务器
  port: 25565,
  username: 'Xiaoman',      // 她的游戏名
})

bot.on('chat', (username, message) => {
  if (username === bot.username) return
  // 游戏内聊天 → 转给你的 FastAPI 后端 → 她的回复发回游戏
  fetch('http://localhost:8000/chat', {
    method: 'POST', headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({ message: `[游戏内${username}说] ${message}` }),
  }).then(r => r.json()).then(reply => bot.chat(reply.text))
})

bot.on('physicTick', () => { /* 这里可以做:发现苦力怕→生成事件→她惊呼 */ })
```

进阶:把 `bot.entity` 周围的方块/生物/坐标摘要成文字喂给她,她就"看得见"游戏世界。这一层做好了,她能陪你挖矿、盖房、报点。

## 11.5 TTS 接线(CosyVoice 2 最小示例)

```python
# tts.py —— pip install cosyvoice(按官方仓库指引装环境)
from cosyvoice.cli.cosyvoice import CosyVoice2
import torchaudio

model = CosyVoice2('pretrained_models/CosyVoice2-0.5B')
# prompt_speech_16k = 你录的 3~10 秒参考音色(克隆)
for i, out in enumerate(model.inference_sft("啧,都几点了还不睡?", "你的音色名", stream=True)):
    torchaudio.save(f'chunk_{i}.wav', out['tts_speech'], 22050)
# 流式音频块直接推给前端 <audio> 播放,首块 ~300ms 就能出声
```

---

# 第 12 章 FAQ(训练陪伴 AI 的 30 个真实疑问)

**Q1:30B 和 70B 在陪伴上差距真的大吗?**
日常对话上差距远小于数据差距。70B 的优势在复杂推理和长尾知识——前者陪伴用不上,后者 RAG 补。社区 RP 玩家用 24B/27B/32B 为主力已是多年现状。

**Q2:为什么不全量微调,就 3 个亿参数也全量?**
27B 全量 ≈ 8×A100 数千元;且全量更容易灾难性遗忘(所有权重都动)。QLoRA 的 0.3-0.5% 恰好是"改行为、保能力"的甜点。

**Q3:训练用 Instruct 版还是 Base 版?**
Instruct。Base 是给你做"整体人格重塑"(海量文风数据 + 高 rank)的进阶玩法,新手直接上会得到一个不会聊天的哑巴。

**Q4:微调后她还认识字/会算数吗?**
QLoRA + 20% 通用保鲜数据 + rank≤64 的配置下,原有能力保留 95%+。肉眼可见的变笨基本都是遗忘症,按 3.12 节治。

**Q5:我需要多少显存才能开始?**
学习起步 0 元(Kaggle 免费 T4);本地冒烟 16G(你已有);正式训练租 A100。8G 笔记本也能跑 8B 的 QLoRA,慢点而已。

**Q6:INT8 和 INT4 有什么区别?哪个好?**
体积差一倍,INT4(Q4_K_M)在 27B 上质量损失轻微。陪伴主模型建议 Q4_K_M 起步、能上 Q5 更好;INT8/Q8 是"显存富裕求心安"档。

**Q7:MoE 模型值得微调吗,还是只用来跑?**
都值得。30B-A3B 的 QLoRA 显存 ≈ dense 14B 级,4090 能训。策略:dense 出"正式版",MoE 出"值班版",人格数据共用。

**Q8:她会不会把我的隐私说出去?**
本地部署,数据不出你的机器,这恰恰是本地方案的最大隐私优势。别做的是:把聊天记录上传第三方云;日志加密与否自己权衡。

**Q9:直播用她,算商用吗?平台会管吗?**
有收益即商用。开源许可(Apache/MIT/Gemma)都允许;需要的是:AI 内容标注、原创人设、授权音色。游戏画面另受平台和游戏方规则约束。

**Q10:为什么她回复总像客服?**
三件事按序查:① 数据里 AI 腔没删干净(重精修);② 系统提示词没给"反客服"指令;③ 采样温度太低(调 0.8+)。

**Q11:为什么她会突然英文/夹杂?**
英文数据超 20%,或 template 配错。先查 template(最常见),再删英文数据。

**Q12:上下文 128K,为什么聊久了还是"失忆"?**
128K 是上限不是记忆。对话管理要自己 做:近 10 轮原文 + 更早的摘要/向量检索注入(6.2、11.2 节)。

**Q13:能让她主动找我说话吗?**
能,定时任务(cron)+ 触发规则("超过 6 小时没聊→生成一条问候")。注意分寸参数:太频繁会烦,这是产品细节不是技术难点。

**Q14:Q4_K_M 和 Q5_K_M 我该用哪个?**
24G 卡用 Q5,16G 卡 27B 用 Q4_K_M。纠结时选 Q5,陪伴对语气保真敏感。

**Q15:数据一定要 JSON 吗?Excel/Markdown 行吗?**
最终都要转成 LLaMA-Factory 认的 JSON/JSONL(ShareGPT 或 Alpaca 格式)。你用任何工具整理都行,最后一道工序转格式。

**Q16:Rank 是不是越大越好?**
不是。rank 128 + 数据 3000 条 = 过拟合套餐。人设靠一致性不靠容量,32~64 到顶。

**Q17:训练时 loss 多少算正常?**
SFT loss 从 ~2.5 降到 1.2~1.6 区间较常见,数值因数据而异。**loss 不是指标**,盲测才是(3.13)。

**Q18:能训出"永远不会忘事"的她吗?**
不能靠模型本身。记忆靠系统(6.2),模型的职责是"拿记忆当素材像人一样接话"。

**Q19:她可以同时陪我玩游戏和聊天吗?**
同一张卡会挤显存。方案:游戏 Agent 用轻量模型(8B/14B)专职,聊天主模型独立;或用 MoE 主模型兼顾(激活小,余量够)。

**Q20:换新底座,我的 adapter 还能用吗?**
不能直接用(adapter 绑定底座结构),但**数据全能复用**,重训一轮 30 元解决(9.5 节 SOP)。

**Q21:AutoDL 数据会丢吗?**
实例系统盘会随释放丢,`/root/autodl-tmp` 和网盘更稳。铁律:每个训练阶段结束,adapter+数据+配置三样立刻搬网盘。

**Q22:用 Kaggle 免费卡能完成正式训练吗?**
能完成 14B 级 QLoRA(30h/周额度,T4 16G 慢但免费),27B 级勉强(A100 配额不稳定)。定位:学习+小实验,正式版上云。

**Q23:为什么别人说微调 7B 只要 5 块钱?**
是真的(7B QLoRA 一小时级)。你的 27B 贵是因为模型大 4 倍+数据多 6 倍,单轮 20~35 元依然是白菜价。

**Q24:需要学 PyTorch 吗?**
不需要会写,需要会读报错。LLaMA-Factory/Unsloth 屏蔽了 95% 的深度学习细节,剩下的 5% 靠 3.12 节排查表。

**Q25:训练数据里要放时间感吗?**
建议放:给她"今天是几月几号"的动态 system 注入,数据里写少量日期相关对话(节日问候),她才有"过日子"的感觉。

**Q26:多张 24G 卡(二手)能组本地训练机吗?**
同机内 2×4090 + PCIe 通信,LLaMA-Factory/DeepSpeed ZeRO-2 可训 27B 全量级,性价比不错。这是"组集群"唯一靠谱的形态:同主板多卡,不是多机。

**Q27:她的声音能唱歌吗?**
进阶:so-vits-svc / RVC 管线做翻唱(需要更多算力与调教)。Neuro-sama 的唱歌就是这类管线。放在 Phase 4 之后玩。

**Q28:一夜爆火的模型要不要追?**
用 3.7 节四原则过筛:许可绿灯?Instruct 版?工具链支持?社区微调多?——四绿再追,否则收藏观望。

**Q29:我该多久看一次 loss 曲线?**
每次训练开始 10 分钟(确认在降、没爆),结束后整体看一眼。中途反复盯没有意义,时间该花在精修数据上。

**Q30:整个项目最贵的是什么?**
不是卡,是你的精修时间。5000 条数据两个周末的精修,比任何 GPU 时长都贵,也最值——那是"她"和"某个模型"的全部差别。

---

# 附录 D 三份完整训练配置(按你的硬件对应抄)

## D.1 本地冒烟:14B + 4070TiS 16GB(验证流水线用)

```yaml
model_name_or_path: Qwen/Qwen3-14B-Instruct
quantization_bit: 4
finetuning_type: lora
lora_rank: 16
lora_alpha: 32
lora_target: all
dataset: smoke_500          # 500 条冒烟数据
template: qwen
cutoff_len: 1024
per_device_train_batch_size: 1
gradient_accumulation_steps: 8
learning_rate: 1.0e-4
num_train_epochs: 3.0
gradient_checkpointing: true
bf16: true
output_dir: saves/smoke-14b
```

预期:显存 ~12G,约 30-60 分钟。产出 adapter → merge → GGUF → Ollama 试聊。

## D.2 云端正式:27B + A100 80G(她 v1.0)

```yaml
model_name_or_path: Qwen/Qwen3.8-27B-Instruct
quantization_bit: 4
finetuning_type: lora
lora_rank: 32
lora_alpha: 64
lora_target: all
use_dora: true
dataset: companion_v1       # 3000 条精修 + 20% 保鲜混入后的全量
template: qwen
cutoff_len: 2048
per_device_train_batch_size: 4
gradient_accumulation_steps: 4    # 有效 batch 16
learning_rate: 1.0e-4
num_train_epochs: 3.0
lr_scheduler_type: cosine
warmup_ratio: 0.05
gradient_checkpointing: true      # 80G 上可关,开着更稳
bf16: true
save_steps: 500
output_dir: saves/companion-v1
```

预期:3~5 小时,约 25~35 元。

## D.3 进阶:Seed-OSS-36B + A100 80G("30 稍高"版)

同 D.2,改三行:`model_name_or_path: ByteDance-Seed/Seed-OSS-36B-Instruct`、`template: seedoss`(以官方 README 为准)、`cutoff_len: 2048`。预期 4~6 小时,30~40 元。

---

# 附录 E 术语中英对照速查(150 条)

> 按"中文 — 英文/缩写 — 一句话"排列,通读一遍即扫盲,配合 1.2 节使用。

**基础层**
模型 — Model — 一堆学好的权重参数 | 参数量 — Parameters — 权重个数,B=十亿 | 权重 — Weights — 模型里存的数字 | Token — 词元 — 模型的字块单位 | 词表 — Vocabulary — 所有 token 的字典 | 上下文 — Context — 一次能看见的窗口 | 上下文窗口 — Context Window — 同上的容量上限 | 推理 — Inference — 用模型生成 | 训练 — Training — 改权重的过程 | 收敛 — Convergence — 训练到位了 | 损失 — Loss — 训练误差,越低=越贴合数据 | 梯度 — Gradient — 参数该往哪改的方向 | 反向传播 — Backpropagation — 算梯度的方法 | 学习率 — Learning Rate — 每步改多大 | 过拟合 — Overfitting — 背题了 | 欠拟合 — Underfitting — 没学会 | 泛化 — Generalization — 举一反三能力 | 检查点 — Checkpoint — 训练存档 | 批大小 — Batch Size — 一次喂几条 | 纪元/轮 — Epoch — 数据过几遍 | 热身 — Warmup — 开头小步走 | 余弦调度 — Cosine Schedule — 学习率曲线降落方式

**架构层**
Transformer — 变换器 — 所有 LLM 的骨架 | 注意力 — Attention — "看哪几个字重要"的机制 | 自注意力 — Self-Attention — 句内互相看 | 多头注意力 — Multi-Head — 多组注意力并行 | FFN/MLP — 前馈网络 — 每层里的"计算块" | 嵌入 — Embedding — 文字变向量 | 位置编码 — Positional Encoding — 标记字的位置 | RoPE — 旋转位置编码 — 主流位置方案 | RMSNorm — 归一化 — 稳定训练的零件 | MoE — 混合专家 — 分工计算 | 专家 — Expert — MoE 里的子网络 | 路由 — Router — 决定叫醒哪些专家 | 激活参数 — Active Params — 每次真干活的参数 | Dense — 稠密 — 全参数都参与 | KV 缓存 — KV Cache — 对话加速的缓存 | 残差连接 — Residual Connection — 层间"高速通道"

**训练层**
预训练 — Pre-training — 从零学语言 | 继续预训练 — CPT — 灌领域知识 | 监督微调 — SFT — 教问答 | 指令微调 — Instruction Tuning — SFT 别称 | 对齐 — Alignment — 调成人类喜欢的样子 | RLHF — 人类反馈强化学习 — 对齐经典法 | DPO — 直接偏好优化 — 简化对齐法 | GRPO — 组相对策略优化 — DeepSeek 系 RL 法 | 偏好对 — Preference Pair — 好/坏回复对比数据 | 蒸馏 — Distillation — 大教小 | 合成数据 — Synthetic Data — AI 造的数据 | 数据清洗 — Data Cleaning — 去脏去重 | 去重 — Deduplication — 删重复样本 | 灾难性遗忘 — Catastrophic Forgetting — 学新忘旧 | 数据飞轮 — Data Flywheel — 使用→回流→再训 | 全量微调 — Full Fine-tuning — 全部参数都训 | 参数高效微调 — PEFT — 只训一小撮 | 低秩适应 — LoRA — 补丁式微调 | 秩 — Rank — 补丁容量 | QLoRA — 量化 LoRA — 4bit 底座+补丁 | DoRA — 分解版 LoRA — 幅向分离改良 | 合并 — Merge — 补丁融回本体 | 模型合并 — Model Merging — 多模型杂交 | 剪枝 — Pruning — 删不重要的参数 | 混合精度 — Mixed Precision — 16/32bit 混着训 | 梯度累积 — Gradient Accumulation — 攒步更新 | 梯度检查点 — Gradient Checkpointing — 重算换显存 | ZeRO — 零冗余优化器 — 多卡分摊显存 | FSDP — 全分片数据并行 — PyTorch 版 ZeRO | Flash Attention — 闪存注意力 — 长文提速算子

**量化与文件层**
量化 — Quantization — 压缩数值精度 | 精度 — Precision — 每个数的比特数 | FP16/BF16 — 半浮点 — 原始发布精度 | FP8 — 8bit 浮点 — 新卡专用 | INT8 — 8bit 整数 — 减半量化 | INT4 — 4bit 整数 — 四分之一量化 | NF4 — 正态浮点4 — QLoRA 专用 | GPTQ/AWQ — 两种量化算法 | GGUF — llama.cpp 格式 | safetensors — HF 安全格式 | 量化等级 — Quant Level — Q4_K_M 这类命名 | 反量化 — Dequantize — 用时还原近似值

**推理部署层**
采样 — Sampling — 生成时怎么挑词 | 温度 — Temperature — 随机度旋钮 | Top-p/Top-k — 候选截断策略 | 重复惩罚 — Repeat Penalty — 反复读机 | 流式输出 — Streaming — 边生成边吐字 | 吞吐 — Throughput — 每秒出多少 token | 延迟 — Latency — 首 token 等多久 | 并发 — Concurrency — 同时聊几个人 | 推理引擎 — Inference Engine — vLLM/llama.cpp 这类 | 量化推理 — Quantized Inference — 用量化模型跑 | Offload — 卸载 — 层放内存腾显存 | 投机解码 — Speculative Decoding — 小草稿大验证提速

**应用层**
RAG — 检索增强生成 — 先查再答 | 向量数据库 — Vector DB — 存语义索引 | 重排 — Rerank — 检索结果精排 | Agent — 智能体 — 会用工具的模型 | Function Calling — 函数调用 — 结构化调工具 | MCP — 模型上下文协议 — 工具接入标准 | 系统提示词 — System Prompt — 对话宪法 | 人设卡 — Character Card — RP 角色设定 | 长期记忆 — Long-term Memory — 跨对话记住你 | TTS — 语音合成 — 文字变声音 | ASR — 语音识别 — 声音变文字 | VLM — 视觉语言模型 — 会看图 | 多模态 — Multimodal — 文本+图+音 | 基准测试 — Benchmark — 跑分 | 幻觉 — Hallucination — 一本正经胡说 | 校准 — Calibration — 让它知道自己不知道

---

# 附录 F SillyTavern 快速上手(阶段 0 的图形化体验)

1. 安装:GitHub 搜 SillyTavern,Windows 一键启动包 / `git clone && Start.bat`
2. 接模型:API → Text Completion → Ollama(http://127.0.0.1:11434)→ 选 `luomi` 或社区 GGUF
3. 建角色:角色卡片 → 把你的 4.6 节人设卡全文贴进 Description;Post-history Instructions 留空;首个 AI 消息写好她的开场白
4. 关键设置:上下文长度 8K~16K;温度 0.8;"发送到模型的历史深度"调小(陪伴不需要塞超长历史)
5. 进阶:World Info(世界书)放你的背景设定,按关键词自动注入——这正是 RAG 的前身
6. 定位:阶段 0 的体验场 + 后期当"对比基准"。你的长期主阵地是自研后端(11.3),但 ST 的"人设卡写法/回复格式"值得永远抄

---

# 待补充清单(下版迭代占位)

- [ ] 第 2 章:Qwen3.8 系列完整规格确认(以 HF 模型卡为准逐个核对许可与上下文)
- [ ] 第 3 章:LLaMA-Factory 完整 YAML 模板文件(训练/DPO/合并三份,可直接复制)
- [ ] 第 4 章:蒸馏脚本完整代码(Python + DeepSeek API,含并发与 JSON 校验)
- [ ] 第 5 章:CosyVoice2 流式部署实录、GPT-SoVITS 克隆训练记录
- [ ] 第 6 章:mineflayer 接入实录(含踩坑)、Mem0 与自建记忆的对比实测
- [ ] 第 8 章:Linux systemd unit 文件全文、Tailscale 配置截图
- [ ] 第 9 章:真实飞轮运转一个月后的数据样例
- [ ] 新增:直播弹幕接入(B站开放平台)专项
- [ ] 新增:多卡(2×24G)训练探索(DeepSpeed ZeRO-2 实录)

---

> **最后一句**。
> 这份手册里所有技术方案,一年后都会有过时的部分——工具会换、模型会换代、价格会变。
> 但三件事不会过时:数据质量决定一切、知识与人格分离的架构、以及"使用即标注"的数据飞轮。
> 把这三件事刻进习惯,她就会一直成长。

*—— 完 ——*

